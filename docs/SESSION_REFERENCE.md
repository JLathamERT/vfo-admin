# VFO SESSION REFERENCE
> **This file is the LEAN live-state hub — updated by the STALENESS AUDIT at the end of every session and read in full by the starter at the start.** Keep it current — it is the only place live state (action counts, version numbers, deno check baseline, tags) is tracked. **History is archived and read on demand:** session-by-session narrative + the version/DB "Prior:" tails → [CHANGELOG.md](CHANGELOG.md); the full numbered gotcha registry (#1–#248) → [GOTCHAS.md](GOTCHAS.md) (this hub keeps only a curated ALWAYS-APPLIES subset). New gotchas go in GOTCHAS.md, incrementing from the max — never renumber.

---

## LIVE STATE *(updated every session by staleness audit)*

| Item | Value |
|------|-------|
| `vfo-admin-api` version | **v625 live** (verified via `list_edge_functions`, 2026-07-20 wrap-up). **v625** = the PFT `us_declined` ("we declined them") path + NEW `automation_load_pft_pipelines` loader (branch `claude/pft-meeting-decline-buttons`, MERGE-PENDING; **`backend-good-2026-07-20-v625` owed post-merge**). **This session** (`claude/vfo-session-setup-57a933` edge worktree) adds NEW action `load_email_previews` — **NOT deployed**, ships as the next version (**expected v626**). _Version tails (v625 PFT-batch detail, v619/v620 quick-wins + race fix, v617/v618 hardening + [Failed Amount], v615-v611) → [CHANGELOG.md](CHANGELOG.md)._ |
| `boldsign-webhook` version | **v40 live** — untouched this session (2026-07-03 — 5th approved extension: additive `Declined`/`Expired`/`Revoked` early-return branch resolving the document through the 5-table cascade, chaining `automation_AGREEMENT_declined` via service-role; deployed with `--no-verify-jwt`; gotcha #180) |
| Action count | **415** in code (5 logins + 410 dispatched handlers) — **+1 this session** (`load_email_previews`, AUTH + **ADMIN_ONLY, NOT tab-gated** — every admin may call it; feeds the per-step email-preview chips). **Live v625 still serves 414 until the owed deploy.** Parity gate: 410 `(c) =>` regs in `dispatch.ts` + 5 logins in `index.ts`. _Prior +1 (`automation_load_pft_pipelines`), +0 (Standard Coaching), +1 (`agreement_pdf_draft`), +1 (`membership_send_reminder`) → [CHANGELOG.md](CHANGELOG.md)._ |
| `deno check` baseline errors | **0** — verified 2026-07-20 on the `claude/vfo-session-setup-57a933` edge worktree (`deno check --no-lock` on the full `index.ts` graph) after the `previews-load.ts` addition. Going-forward gate: any error = fail. **Deploy gotcha:** the local Deno writes `deno.lock` v5 which the Supabase CLI bundler rejects ("Unsupported lockfile version '5'"); `deno.lock` is gitignored — run `deno check --no-lock supabase/functions/vfo-admin-api/index.ts` so it never writes the lockfile, and `rm -f supabase/functions/vfo-admin-api/deno.lock` before `supabase functions deploy` (gotcha #112). |
| DB this session | **No DB changes this session — read-only SELECTs via MCP only** (email_templates inventory + placeholder audit); no DDL, no migration, no data writes; security advisor unchanged. _Prior-session DB ops (PFT `email_templates` ids 196/197 Draft [gotcha #247], Standard Coaching `programs` row #246, membership `20260717210000` [gotchas #240–#241], `email_templates` id 195 Draft, schedule-uniqueness `20260717190000`, `[Failed Amount]` edit) → [CHANGELOG.md](CHANGELOG.md)._ |
| `verify_jwt` | `false` on both functions (config.toml + live registry matched) |
| Supabase project | `ejpsprsmhpufwogbmxjv` ("VFO Showroom"), us-east-2, Postgres 17 |
| Frontend live URL | `https://vfoportal.com/` |
| Frontend deploy state | Latest live tags — VERIFY via `git tag -l 'live-*' --sort=v:refname` / `git tag -l 'backend-good-*' --sort=v:refname` (git is source of truth; note plain PowerShell `sort -V` is NOT version-aware — use git's `--sort=v:refname`). As of 2026-07-20: latest shipped tag is **`live-81-editable-step-dates`**. **NEW this session (`claude/vfo-session-setup-57a933`, MERGE-PENDING, BOTH repos) — per-step email-preview chips:** every email-sending step across 9 admin surfaces now shows a tiny inline "Emails (N)" pill that opens a centered modal listing every template that step can send (live subject/body from `email_templates`, To/Cc/Bcc via `ROLE_LABELS`, variant + member-behalf twin rows, known values substituted / unknown tokens as `[Token]` chips). 12 vfo-react files (2 new shared: `components/shared/StepEmailsChip.jsx` + `components/shared/templateMeta.js`); backend +1 action `load_email_previews` (NOT deployed → next version, expected v626); all chips `!readOnly`-guarded on member-visible tracks; 126 referenced template names verified vs live DB. FE `npm run deploy` owed → next live-N (≥live-82). Gotcha #248. **Prior (`claude/pft-meeting-decline-buttons`, MERGE-PENDING, BOTH repos) — PFT meeting decline-buttons + PFT automation panel + Paul Latham access:** vfo-react FE (5 files: `admin/pft/PFTEngagementTrack.jsx` 3-row decline buttons, NEW `admin/PFTAutomationPanel.jsx`, `pages/AdminPortal.jsx` nav wiring, `pages/ClientDetail.jsx` client prop, `admin/MembersPanel.jsx` Paul-Latham allowlist). **Backend already deployed (v625)**; FE `npm run deploy` owed → next live-N (≥live-82). Gotcha #247 (+#245 updated). **Prior (`claude/standard-coaching-program`, MERGE-PENDING, BOTH repos) — Standard Coaching, the 5th MSM program:** vfo-react FE (4 files: `admin/MSMTracking.jsx`, `admin/MembersPanel.jsx`, `member/MemberMSMTracking.jsx`, `pages/MemberPortal.jsx`) + one edge migration seeding the `programs` row (id=5). Copy of Advanced Coaching minus the Renewal tab; MSM Home "Standard Meetings" counter (+ fixed the dead Advanced counter to read `coaching_meetings`); optimistic no-flicker program toggles; member MSM nav ordered to match the admin toggle list + tab renamed "MSM Tracking"→"MSM". `npm run deploy` owed → next live-N (≥live-82). No backend/dispatch/edge-code change. Gotcha #246. _Prior sessions → [CHANGELOG.md](CHANGELOG.md)._ |

---

## 🔒 SECURITY INVARIANTS *(check on EVERY DB or handler change — these must NOT regress)*

Non-negotiables from the 2026-06-18 security remediation. Re-check any time you add a table, policy, handler, or function — do not let a new feature quietly undo these:

1. **Every new public table = RLS deny-all + verify.** Create it with `enable row level security` + `create policy "Deny all access" … for all to public using (false)` in the SAME migration, then confirm with an anon-key probe (`curl --head -H "apikey:<anon>" -H "Authorization:Bearer <anon>" -H "Prefer:count=exact" "<base>/rest/v1/<table>?select=*"` → must be `Content-Range: */0`). The anon key is PUBLIC and `anon`/`authenticated` hold full table grants, so RLS is the ONLY thing protecting the DB. (gotcha #141)
2. **Every member-facing AUTH handler = caller-ownership check on `auth.callerMemberNumber`, NEVER a body id.** The edge fn runs as service-role (bypasses RLS), so a handler that reads/writes by a body `client_id`/`ciq_id`/`enrollment_id` without re-checking ownership is an IDOR. Reuse `utils/client-ownership.ts` (`denyIfNotOwnClient`/`denyIfNotOwnEnrollment`) + `actions/ciq/shared.ts denyIfNotOwnCiq`. Role-gates are deny-by-OMISSION + matched by EXACT action name. (gotcha #142)
3. **Every new SECURITY DEFINER function = pin `search_path` + `REVOKE EXECUTE … FROM public`.** (gotcha #143)
4. **After ANY DB/table/policy/function change → run the Supabase security advisor (`get_advisors`, type `security`) and confirm it's GREEN** — this is the automated net that catches a regressed (anon-reachable) table before it ships. It is part of the VERIFICATION GATE below.

Full audit history + live-verified detail: the 2026-06-18 security-remediation entry in [CHANGELOG.md](CHANGELOG.md) + gotchas #141–#146.

---

## REPOS

**Frontend** (`vfo-react`)
- GitHub: `github.com/JLathamERT/vfo-portal`
- Local: `C:\vfo-react`
- Live: `https://vfoportal.com/`
- Stack: Vite + React 18 + react-router-dom v6, gh-pages deploy
- No backend code; no tests; no CI/CD
**Edge Functions** (`vfo-edge-functions`)
- GitHub: `github.com/JLathamERT/vfo-edge-functions`
- Local: `C:\vfo-edge-functions`
- Stack: Deno 2 / Supabase Edge Runtime
- Deployed Edge Functions — two primary (below) plus two one-off helpers (verify_jwt off, no business logic — they read templates/BoldSign and create Gmail drafts on demand): `draft-agreement-pdfs` (v1) + `boldsign-template-fields` (v1), added 2026-06-01. (Three more ad-hoc sample-PDF drafters — `draft-onboarding-agreement-pdfs`, `draft-map1-sample-pdfs`, `draft-tax-sample-pdf` — were created and **deleted** during the 2026-06-01 agreement-coordinate session; re-deploy a throwaway from that pattern if you need to eyeball template PDFs again.)
  - `vfo-admin-api` — 88-line `index.ts` orchestrator + ~202 modular handlers in `actions/<group>/*.ts` (incl. `actions/advisor/` for Advisor Onboarding + `actions/accountant/` for Accountant Onboarding + `actions/msm/pip-*.ts` for the PIP Meetings purchase chain) + `router/dispatch.ts` (PUBLIC_HANDLERS + AUTH_HANDLERS) + `router/webhooks.ts` (Stripe + BoldSign by shape; Stripe handler cascades by `stripe_customer_id` lookup: `pipeline_map1` → `client_tax_plans` → `advisor_onboarding` → `client_priority_tracks` (PIP) → `accountant_onboarding` → `specialist_onboarding` (background-check, by `bg_stripe_customer_id`); Stripe handler branches on `metadata.pipeline` ∈ {`'MAP 1'` implicit, `'TAX'`, `'ADVISOR_ONBOARDING'`, `'PIP'`, `'ACCOUNTANT_ONBOARDING'`, `'SPECIALIST_ONBOARDING'`} and `metadata.payment_kind` ∈ {`'retainer'`, `'implementation'`, `'onboarding'`, `'purchase'`, `'background_check'`}. Also handles `payment_intent.payment_failed` for SPECIALIST_ONBOARDING (marks `bg_payment_status='failed'` + Tracy FYI)) + `middleware/auth.ts` + `utils/` (incl. `program-label.ts` for program-aware client-visible labels, `pip-html-templates.ts` for PIP invoice/receipt PDFs, `specialist-html-templates.ts` for specialist invoice/receipt PDFs, `gmail-draft.ts` shared Gmail-draft helper), `constants/`, `types/`, `integrations/`
  - `boldsign-webhook` — standalone function. Extended four times with prior user approval: (a) tax-plan fallback routing after MAP1 miss, (b) advisor_onboarding fallback routing after tax miss (Phase 3), (c) `_stripecustomer` chain on advisor Completed event (Phase 4), (d) accountant_onboarding fallback routing after advisor miss + `_stripecustomer` / `_ceocountersign` chains on Completed / Signed events. PIP Meetings does NOT use BoldSign (no agreement step in the PIP flow). **Deploy with `--no-verify-jwt` flag (mandatory) — without it BoldSign gets 401.**
**Supabase Storage public buckets:**
- `tax-agreements` — contains `tax-planning.pdf` (static Undecided-path agreement attachment for tax)
- `map1-agreements` — contains `proactive-lite.pdf`, `proactive-core.pdf`, `proactive-max.pdf` (static Undecided-path agreement attachments for MAP 1; Max PDF suppressed when admin ticked "N/A" for Max). **Added this session:** `proactive-lite-member.pdf`, `proactive-core-member.pdf`, `proactive-max-member.pdf` — the member-paid-on-behalf variants, attached by `pipfu-decision.ts` when `member_paying_on_behalf=true` (the `-member` suffix is selected in code via `sfx`).
- `map1-assets` — **NEW this session** (public). Contains `PIP-FollowUp-Presentation.pdf`, the "standard presentation" linked ("click here") in the MAP 1 Undecided + Agreement-sent emails (templates ids 3,4,5,111,117,118). This **replaced a hardcoded Google Drive link** (`1bKcAGuV...`) that was embedded in those email bodies. Public URL: `…/storage/v1/object/public/map1-assets/PIP-FollowUp-Presentation.pdf`.
- `advisor-onboarding-agreements` — contains `Advisor_Implementation_Agreement.pdf` (static Undecided-path agreement attachment for advisor onboarding, fetched by `actions/advisor/decision.ts`; handler falls back gracefully to a no-attachment email when the PDF is missing)
- `accountant-onboarding-agreements` — contains TWO partnership-branched PDFs: `Accountant_Implementation_Agreement_Partnership.pdf` and `Accountant_Implementation_Agreement_No_Partnership.pdf`. `actions/accountant/decision.ts` picks one by `ob.accountant_partnership` (=== `'Accountant Partnership'` → Partnership PDF, else No-Partnership — same dropdown the Yes-path send-agreement uses to pick the BoldSign template), with graceful no-attachment fallback (v349, 2026-06-01; see gotcha #58)
- `specialist-onboarding-assets` — public bucket for Specialist Onboarding emails (added 2026-06-02): `onboarding-process.png` (the 5-step process diagram embedded inline in the Stage 1, Stage 2 & Step 3 receipt emails), plus `VFO-Specialist-Agreement.pdf` + `revenue_share_examples.pdf` + **`VFO-Specialist-Onboarding-Presentation.pdf`** (all three attached to the Stage 1 "yes/continue" email by `actions/onboarding/prelim-email.ts`, fetched as static PDFs — graceful skip if a file is missing). The presentation PDF was added this session; the agreement PDF was **refreshed** (re-rendered from the current `agreement_templates` id=12 HTML after the effective-date removal). NOTE: this static bucket agreement PDF is a **reference copy only** (placeholders left raw) — the BoldSign signing flow renders the agreement HTML→PDF fresh per-specialist (see gotcha #79), it does NOT use this bucket file. `agreement_templates` id=12 now has a populated `field_map` + `boldsign_template_id`.
- `vfo-widget` — public bucket serving the embeddable public showroom widget `vfo-widget.js` (source now tracked at `widget/vfo-widget.js` in the edge repo). External sites embed it as `<script src=".../vfo-widget/vfo-widget.js?v=NN" data-vfo-key="…">`; `?v=NN` is only a cache-buster (Storage ignores it and the object is served `Cache-Control: no-cache`), so **replacing the bucket file propagates to every embedding site on next load — no per-site edits, no `?v` bump needed**. The widget reads `experts` (8 safe columns ONLY — anon is column-restricted, gotcha #201; `vfo_accredited` added 2026-07-16) + `vfo_ecosystem_assignments` via the anon key, and excludes BOTH `Member Services` (internal ecosystem) experts AND `vfo_accredited` (VFO Accredited Professional Specialist) experts (gotcha #231).
PIP Meetings does NOT use any storage bucket (no static agreement PDF). Add new public buckets here as built.

**Supabase Storage PRIVATE buckets:**
- `specialist-dd-materials` — **private** bucket for Specialist Onboarding Due Diligence Checklist uploads (added 2026-06-04). Files namespaced by onboarding id (`<onboarding_id>/<slot>/<rand>_<filename>`). The specialist's public DDC form (`/specialist-ddc`) uploads via **signed upload URLs** minted by `automation_SPECIALIST_ddcuploadurl` (browser PUTs multipart FormData straight to the bucket — no anon write policy). Admins view/download via short-lived (300s) signed download URLs minted by `automation_SPECIALIST_ddcdownload` (path prefix-checked against the onboarding id).
- `client-tax-returns` — **NEW this session** (private). The client **Vault → Sensitive Documents** (tax returns). Files namespaced by client id (`<client_id>/<rand>_<filename>`). Uploaded by clients via signed upload URLs: the public token page `/tax-upload?token=` (`vault_tax_upload_url`, keyed on `clients.tax_upload_token`) AND the logged-in client portal (`client_vault_upload_url`, section=`sensitive`, scoped to `auth.callerClientId`). Admin access is **allowlist-gated**: every admin can list titles (`vault_tax_list`) but only `isTaxAdmin` admins (Jake/Tim/Tray — `constants/tax-access.ts`) can mint a 300s view link (`vault_tax_download`), upload (`vault_tax_admin_upload_url`), or delete (`vault_tax_delete`).
- `client-documents` — (private). The client **Vault → General Documentation**. Same `<client_id>/...` namespacing. Client uploads via `client_vault_upload_url` (section=`general`); **all admins** can view/add/delete (`vault_gen_list`/`vault_gen_upload_url`/`vault_gen_download`/`vault_gen_delete` — no allowlist gate).
- `specialist-documents` — (private). The **General Documentation** section of the **Specialist portal Vault**. Files namespaced by **`expert_id`** (`<expert_id>/<rand>_<filename>`). Populated on go-live: `automation_SPECIALIST_createspecialist` copies the specialist's DD-checklist files here. The logged-in specialist (scoped to `auth.callerSpecialistId`) uploads/lists/views/deletes via `specialist_vault_upload_url`/`_list`/`_download`/`_delete`; admins via `specialist_vault_admin_*` (Search Specialists → Vault tab — now read **and** write, both sections). **As of this session the specialist vault is two-section** (mirrors the client vault): the `section` body field (`'general'` default / `'sensitive'`) selects the bucket via `specialistBucketFor` in `specialist-vault-upload-url.ts`.
- `specialist-tax-returns` — **NEW this session** (private). The **Tax Documents** (sensitive) section of the Specialist portal Vault; same `<expert_id>/...` namespacing. Starts empty (DD files go to General); the specialist adds tax docs after login. Same handlers as `specialist-documents`, `section='sensitive'`.
- `member-vault` — (private). The **General Documentation** section of the **Member Vault** (member portal + admin Members → Vault tab). Namespaced by `member_number`. **This session the member vault moved from base64 (`vault_upload`) to signed-URL uploads** (`vault_upload_url`) + on-demand download (`vault_download`); `vault_list` returns `{sensitive,general}`, `vault_delete` keyed by path+section.
- `member-tax-returns` — (private, 50 MB). The **Tax Documents** (sensitive) section of the Member Vault; same `<member_number>/...` namespacing + handlers, `section='sensitive'`.
- `member-ert-docs` / `client-ert-docs` / `specialist-ert-docs` — **NEW 2026-07-09** (private, 50 MB each). The **ERT/VFOS Documentation** third vault section on the member / client / specialist vaults — **admin-managed, read-only for the owner**. Namespaced by `member_number` / `client_id` / `expert_id`. Bucket map + `listErt()` live in `actions/vault/ert-shared.ts` (`ERT_BUCKETS`, **separate** from the general/sensitive section maps). Admins add/remove via the unified admin-only set `admin_ert_{list,upload_url,download,delete}` (`actions/vault/admin-ert.ts`, `{entity, key}`); the owner sees an `ert` array from `*_vault_list` + can download `section='ert'` but every owner write handler REJECTS `section='ert'`. Signed BoldSign agreements land here automatically on sign+pay (`utils/ert-agreement-copy.ts`). Gotchas #204–#206.

**Architecture documentation lives in `vfo-react/docs/`** — yes, in the frontend repo. It documents the backend from the frontend-team perspective. PIP Meetings has a dedicated flow doc at `docs/flows/pip-meetings.md`.

---

## CURRENT STATE

- **Per-step email-preview chips — inline "Emails (N)" pill + modal across 9 admin surfaces** (2026-07-20, branch `claude/vfo-session-setup-57a933` BOTH repos. **+1 action (415)** (`load_email_previews`, AUTH + ADMIN_ONLY, **NOT tab-gated**), deno 0, `boldsign-webhook` untouched (v40). **NO DB changes** — read-only SELECTs via MCP only (security advisor unchanged). **Backend NOT deployed** — ships as next version (expected v626); **Frontend NOT deployed** — `npm run deploy` owed → next live-N (≥live-82). Build clean; user approved the MAP 1 modal + inline-pill placement + reactive values, full rollout build-verified. Gotcha **#248** (+#181 location amended). **MERGE-PENDING both repos.**)
  - **The chip + modal.** Every email-sending step now renders a tiny inline "Emails (N)" pill next to the step label; clicking opens a centered modal listing every template that step can send — live subject/body from `email_templates`, To/Cc/Bcc resolved via `ROLE_LABELS`, variant labels, and member-behalf twin rows auto-appended. Known values (client/member/PF/advisor/accountant/specialist names, PFT meeting phrases mirroring `actions/pft/meeting-email.ts`, PIP scheduled date, MAP1 service level, membership amount/cadence/renewal) substitute as plain text via a `context` prop; unknown tokens render as bracketed `[Token]` chips (tag/attribute segments untouched). All chips `!readOnly`-guarded on member-visible tracks.
  - **9 surfaces (12 react files).** MAP 1 `map1/ClientTrackViewV2.jsx`, Tax `tax/TaxPrioritiesTab.jsx` (both programs), Regular `regular/RegularPrioritiesTab.jsx`, PIP `pip/PipMeetingsTab.jsx`, PFT `pft/PFTEngagementTrack.jsx`, `admin/AdvisorOnboarding.jsx`, `admin/AccountantOnboarding.jsx`, `admin/SpecialistOnboarding.jsx`, `admin/MembershipFeesPanel.jsx`. `ClientDetail.jsx` now also passes `client` to `PipMeetingsTab`.
  - **Shared modules + META extraction.** NEW `components/shared/StepEmailsChip.jsx` (pill + modal; lazy `loadCachedAction('load_email_previews', {pipeline})` with silent fallback to `automation_load_email_templates`) + NEW `components/shared/templateMeta.js` (`mv`/`TEMPLATE_META`/`ROLE_LABELS` moved VERBATIM out of `EmailTemplatesPanel.jsx`, which now imports them; plus currently-unused `TOKEN_LEGEND`/`friendlyToken`).
  - **Backend loader + gating rationale.** NEW AUTH action `load_email_previews` (`actions/email-templates/previews-load.ts`) selects id/pipeline/template_name/subject/body/to_list/cc_list/bcc_list/active/send_mode, active=true, optional `pipeline`/`pipelines` body filter; registered in `router/dispatch.ts` after `automation_load_email_templates`, gated via `ADMIN_ONLY_ACTIONS` in `constants/role-gates.ts` — deliberately **NOT** in `TAB_ACTIONS` so EVERY admin may call it (unlike the superadmin-only automation loader). +1 action → 415 in code (410 dispatched + 5 logins); live v625 still serves 414 until the owed deploy.
  - **Verification done.** All 126 referenced template names verified against the live DB; `deno check` 0 errors on this worktree; frontend build clean. Deploy owed both repos (FE `npm run deploy` → ≥live-82; backend → v626).

- **PFT meeting decline-buttons + "we declined them" email path + PFT Automation & Config panel + Meeting-1 tracking-owner gate + Paul Latham access** (2026-07-20, branch `claude/pft-meeting-decline-buttons` BOTH repos. **+1 action (414)** (`automation_load_pft_pipelines`), deno 0, `boldsign-webhook` untouched (v40). **Backend DEPLOYED v625** (3 deploys v622→v625); **Frontend NOT deployed** — `npm run deploy` owed → next live-N (≥live-82). DB: two `email_templates` rows seeded via MCP (ids 196/197, Draft); security advisor GREEN. User click-through confirmed decline drafts + tab + Paul access in dev. Gotchas **#247** (+#245 updated). **MERGE-PENDING both repos.**)
  - **Meeting decline buttons (`admin/pft/PFTEngagementTrack.jsx`).** All three meeting-confirmation steps (`MeetingStep`) are now a 3-row button layout — Row 1 green `Send Email - Date Confirmed` / `Send Email - Date Not Confirmed` (renamed from "Send email (with date)/date not confirmed"), Row 2 red `Send Email - Client Declined` (client declines us; renamed from "Meeting declined - email client") + a NEW "we declined them" button, Row 3 `Complete - NO EMAIL`. The new button opens an inline reason + email-preview card cloned from the Tax 3 decline card. Meeting 1's new button reads **"Send Email - Member Declined"** (chat-variant email, status "Declined by Member"); Meetings 2/3 read **"Send Email - ERT/VFOS Declined"** (meet-variant, status "Declined by ERT/VFOS"). `client` prop now threaded from `ClientDetail.jsx` for the preview name.
  - **Backend (`actions/pft/meeting-email.ts`, v625).** `automation_PFT_meetingemail` decision vocab gained `us_declined` (requires `decline_reason`, injects `[DECLINE_REASON]`), template chosen by meeting (`PFT_meeting1_member_declined` id 197 for Meeting 1 else `PFT_meeting_ert_declined` id 196). Meeting-2 discovery-form section now attaches only on confirm decisions (guard tightened so `us_declined` never mints a form token).
  - **PFT Automation & Config panel (NEW `admin/PFTAutomationPanel.jsx` + `AdminPortal.jsx` `pft_pipeline` section).** Automation & Config → Partnership Fast Track. StepKit-styled, lists one row per client with ANY PFT email activity (a meeting `client_progress` row OR a `pft_engagement` row) — mirrors how Advisor/Accountant panels only surface in-flow entities. Per-client step cards: Meeting 1/2/3 (selected+sent outcome), Discovery form (sent/submitted/reminders), Decision email (admin pick + Undecided-email client button-click + when), FT follow-up (Confirm/Another-Meeting click + when), Accountant Onboarding handoff (+ "View onboarding →"). Fed by NEW AUTH/admin action `automation_load_pft_pipelines` (`actions/pft/automation-load-pipelines.ts`; dispatch + both role-gate lists). **The panel hero carries the ONLY UI for the `PARTNERSHIP_FAST_TRACK` sandbox toggle** (previously DB-row-only; currently ON).
  - **Meeting-1 gate.** The automation panel's Meeting 1 card only shows when the Initial-Contact "Who is completing the tracking for this accountant?" step = **Member**; the loader returns `tracking_owner` and nulls Meeting 1 (and drops Meeting-1-only clients) when VFOS/unset.
  - **Paul Latham access (`admin/MembersPanel.jsx`).** The VFO Reconciliation (Free) open-gate now allows `platham@elitert.com` (Paul Latham, role admin) alongside superadmin (`canOpenReconciliation`); every other non-superadmin still gets the "Access Denied" flash. Frontend-only (see #245).

- **Standard Coaching — 5th MSM program (copy of Advanced Coaching, no Renewal tab)** (2026-07-20, branch `claude/standard-coaching-program` BOTH repos — vfo-react FE (4 files) + one edge migration; **NO backend/dispatch/edge-code change, +0 actions (413)**, deno function graph untouched, `boldsign-webhook` untouched (v40). DB: `programs` row `Standard Coaching` (id=5) applied live via MCP + migration `20260720000000_standard_coaching_program.sql`; **security advisor GREEN**. **Frontend NOT deployed** — `npm run deploy` owed → next live-N (≥live-82). Build clean; user confirmed working in dev. Gotcha #246. **MERGE-PENDING both repos.**)
  - **Backend is program-agnostic.** `msm_load_programs` returns all `active programs`; enroll/toggle/coaching-meeting/notes handlers key on `program_id`/`enrollment_id`/`program_name`, so the 5th program needed only the data row + FE wiring. FE spots (per gotcha #246): `admin/MSMTracking.jsx` (`PROGRAMS` + `activeSection→key`), `admin/MembersPanel.jsx` (`DEFAULT_MSM_OPTIONS` + render-guard `.includes`), `member/MemberMSMTracking.jsx` (`PROGRAMS` + tab maps), `pages/MemberPortal.jsx` (`PROGRAM_KEYS`×2 + `tabLabels` + `PROGRAM_ORDER`).
  - **Standard = Advanced minus Renewal.** `isCoachingLike = isCoaching || isStandard` hides the training/status/Edit chrome; a separate `isStandard` branch renders Home + Meetings only. Notes (internal/shared) and the coaching Meetings tab work unchanged (program-agnostic).
  - **MSM Home "Standard Meetings" counter + Advanced counter fix.** Both coaching tiles now count COMPLETED rows from `coaching_meetings` per enrollment (the program Meetings tab writes there via `coaching_log_meeting`); the old `member_meetings meeting_type==='Advanced Meeting'` filter was dead (always 0). Counts refresh on entering the Home tab (effect on `activeSection`/`activeTab`) — a just-logged meeting shows without a reload.
  - **Program toggles no longer flash the skeleton.** Enabling/disabling updates local `enabledPrograms` optimistically (no `loadData` reload, no "Saved!" — the toggle is the save); backend is a single `member_program_enabled` upsert/delete. **Member MSM nav** now orders enabled programs by the canonical `PROGRAM_ORDER` (matches the admin toggle list) and the member tab is renamed **"MSM Tracking" → "MSM"**.

- **Admin guardrails — 4 UI gates (MSM-before-programs, superadmin-only advisor open/create, PF-before-client-tracks) + MSM-save skeleton fix** (2026-07-18, `claude/vfo-session-setup-69d588` vfo-react ONLY; frontend-only guardrails, `npm run deploy` owed → next live-N (≥live-82); gotcha #245) — archived in [CHANGELOG.md](CHANGELOG.md).

- **`extra_cc` array-normalization fix + Akele agreement-email recovery (in flight)** (2026-07-18, `claude/extracc-array-fix` edge + react docs-only; **NOT yet deployed** — ships as next backend version; **+0 actions**, deno 0; gotcha #244) — archived in [CHANGELOG.md](CHANGELOG.md). **REMAINING (next session): deploy the fix, then the approved Akele recovery — reset row 108's agreement columns (`boldsign_doc_id`/`c16_sent`/`c17_client_signed`/`c18_ceo_signed`/`c17_followup_sent_date` → NULL), Jake re-submits the PIP decision form (fresh BoldSign doc + proper first-touch agreement email; NOT the signing-reminder nudge — user explicitly rejected that), old BoldSign doc revoke-or-expire (inert either way once unlinked).**

- **Editable step-completion dates (all admin tracking tracks) + PFT "Complete - NO EMAIL" backfill buttons** (2026-07-17, `claude/vfo-session-setup-be14fa` vfo-react ONLY; shared `components/shared/StepDate.jsx`; FE `npm run deploy` owed; gotchas #242–#243) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member Membership Fees — M3 suspension separation + M8 expiring update links (v621 + migration + template)** (2026-07-17, `claude/membership-m3-m8` BOTH repos; backend v621 deployed, migration `20260717210000` live, template id 195 Draft; MERGE-PENDING, `backend-good-2026-07-17-v621` owed; gotchas #240–#241) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member Membership Fees — quick-wins + medium batch: M1/M4 failure bells, mode guards, Payments-page coverage, panel fixes (v619) + webhook-race fix (v620) + schedule-uniqueness migration** (2026-07-17, `claude/membership-quickwins` BOTH repos; backend v619+v620 deployed, migration `20260717190000` live; MERGE-PENDING, `backend-good-2026-07-17-v620` owed; gotchas #238–#239) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member Membership Fees — full 6-track audit + C1/C2/H1/H3 hardening (v617) + `[Failed Amount]` email fix (v618)** (2026-07-17, `claude/membership-fee-hardening` BOTH repos; backend v617+v618 deployed + verified, smoke 5/5; MERGE-PENDING, `backend-good-2026-07-17-v618` owed; gotchas #235–#237) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Accounting tab reorganization — combined revenue/reconciliation panels + submenu reorder + description-column tags removed** (2026-07-17, `claude/accounting-tab-reorg` vfo-react ONLY; `AccountingCombinedPanel.jsx` pill wrapper + `embedded` sub-panels + submenu reorder + `PaymentsTable` tag removal; FE `npm run deploy` still owed → next live-N) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Partnership Fast Track — 90-Day-Plan positive-only progress + live "90 Day Plan:" header + "Accountant" wording + PFT accountant→connected-member auto-link** (2026-07-17, `claude/pft-progress-connected-member` BOTH repos; positive-only `trainingStatus.js`, live `PlanStatusBadge`, PFT "Accountant" wording, `create-member.ts` connected-member auto-link; backend shipped in v617, FE `npm run deploy` owed → live-77; gotchas #233–#234) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Specialist Tax section — per-entry Audit Risk Questionnaire + accredited tag next to the name + Tax polish** (2026-07-16, `claude/specialist-tax-per-entry` vfo-react ONLY; SHIPPED **`live-76-specialist-tax-per-entry`**; tax fields per Tax Planning entry via `seedTaxFromFlat` + mirror-to-flat; Tax Risk Mindset removed portal-wide; Tax Risk Notes edit-only; gotcha #232 update) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Specialist profiles — multiple entries per ecosystem + combined showroom short bio** (2026-07-16, `claude/specialist-multi-ecosystem` vfo-react ONLY + `short_bio` MCP backfill; SHIPPED **`live-75-specialist-multi-ecosystem`**; `experts.ecosystem_content` object→ordered ARRAY read via `entriesFor()` (3 shapes); flat `short_bio` = distinct entry bios joined; gotcha #232) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Accredited Professional Specialist flag — hidden from every Showroom + widget, visible in Specialist Search + Holistic/Tax pickers** (2026-07-16, BOTH repos; SHIPPED **`live-74-vfo-accredited-specialist`** + `vfo-widget.js` bucket re-upload; migration `20260716000000_experts_vfo_accredited` (anon grant 7→8 cols); hide = `MemberShowroom.jsx` + widget filters, NEVER `load_data`; gotcha #231) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Partnership Fast Track 90-Day Plan — "Signed Partnership Fast Supporting Agreements" step removed + admin folding "Watch Video" button** (2026-07-16, vfo-react ONLY + `program_training_tasks` row 71 delete (live data op); SHIPPED **`live-73-pft-watch-video`**) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Payment Continuation — full audit, hardening fixes (v612), revshare visibility + accounting links (v613), System-B tax-row wipe** (2026-07-16, `claude/xenodochial-burnell-fc5c07` BOTH repos; backend v612+v613 DEPLOYED + verified, smoke 5/5; gotchas #228–#230. **Still owed:** FE `npm run deploy` (its react half) + `backend-good-2026-07-16-v613` stamp; user re-enters the 5 wiped tax rows from `C:\Users\jakel_fjetgbx\payment-continuation-inventory-2026-07-16.md`; Terpeluk (59515) needs a Stripe Connect account. **Upcoming REAL charges:** Cruts P2 7/23 card $1,390.63; Burkhart P2 7/31 ACH $1,350; Manseau on first sweep after his setup link (token expires 7/22)) — archived in [CHANGELOG.md](CHANGELOG.md).
- **Tax implementation charge — auto-grab the customer's saved payment method** (2026-07-16, `claude/tax-impl-pm-fallback`; SHIPPED backend v611 — `automation_TAX_charge-implementation` PM fallback lists the Stripe customer's saved methods + persists the hit; TAX still SANDBOX) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Stripe payment-memo overhaul — human-readable memo on EVERY money movement** (2026-07-15 late, `claude/stripe-payment-memos`; SHIPPED backend v610 — all 22 charge/transfer sites carry user-specified memos, convention in `utils/specrev-memo.ts` + per-pipeline formats; +0 actions 413) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Setup-link email — upcoming payment schedule + fine-print security note** (2026-07-15 late, `claude/setup-link-email-schedule`; SHIPPED backend v609 + `backend-good-2026-07-15-v609` tag exists; `migration_send_setup_link` `[PAYMENT_SCHEDULE]` block + `setup_link` template fine-print restyle; +0 actions 413) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Payment-continuation fee waiver + migrated-row dates + fee-free `/connect-card` chooser** (2026-07-15 late, `claude/migration-feefree-dates`; SHIPPED backend v608 + frontend `live-71-migration-feefree-dates`; migration `20260715200000_card_fee_waived`; gotcha #227) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Agreement-templates → PDF → Gmail-draft tool (`agreement_pdf_draft`)** (2026-07-15, `claude/tmp-html-pdf-draft`; SHIPPED — v607 rename deployed + `backend-good-2026-07-15-v607` tag exists; superadmin-only standalone utility, +1 action → 413; invoke server-side via `pg_net` so the token never leaves the DB — full how-to + memory `project_agreement_pdf_draft_tool`) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Membership Fees Phase 4 — Outstanding "Send reminder email" button + auto-send setup email on plan create** (2026-07-15, `claude/membership-phase4-reminder`; SHIPPED backend v605 + frontend deployed in the live-70/71 window; `membership_send_reminder` +1 action → 412; smoke 5/5 post-v605. Still owed: membership go-live flips — `MEMBER_MEMBERSHIP` sandbox→live, LIVE Stripe events, 3 templates Draft→Send, `advisor_model` tags — user's call; user deletes test Gmail drafts + sandbox Stripe customers) — archived in [CHANGELOG.md](CHANGELOG.md).

- **No confirmation email on automatic charges — MAP 1 Q2-4 + Tax implementation send receipt-only** (2026-07-15 late, `claude/vfo-session-setup-b40878`; SHIPPED backend v604 + frontend deployed, tagged `live-69-no-autocharge-confirmations`/`backend-good-2026-07-15-v604`; receipt-on-clear is the single client email for automatic charges; P1/retainer confirmations unchanged; gotcha #132 partially superseded) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Tax-returns / ROI-custom / profile-save / PIP-time session — four independent changes** (2026-07-15, `claude/tax-returns-roi-profilesave` BOTH repos; DEPLOYED backend **v603** + frontend; 4 additive migrations `20260715000000`–`20260715003000`; +1 action → 411; gotchas #225–#226; owed: `live-67-tax-returns-roi`/`backend-good-2026-07-15-v603` stamps + flip `TAX_request_returns` template Draft→Send + delete orphan edge dir `infallible-darwin-811991`) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Advisor/Accountant Onboarding — Evan+Bridger team members, PFT PF handoff, warmed reminders, extra-meeting mechanism** (2026-07-14 late, `claude/vfo-session-setup-2447a4`; SHIPPED backend v601 + frontend deployed; migration `20260714230000_onboarding_extra_meeting`; +2 actions → 410; gotchas #223–#224; owed: post-v601 smoke re-run + `live-66-onboarding-extra-meeting`/`backend-good-2026-07-14-v601` stamps + user deletes Testy-Extrameeting Gmail drafts/BoldSign doc) — archived in [CHANGELOG.md](CHANGELOG.md).

- **PIP Meeting presentation — dropdown auto-fills a read-only date** (2026-07-14, `claude/pip-presentation-autodate` vfo-react ONLY; SHIPPED react #127, deployed 2026-07-14 late; single `PipMeetingsTab.jsx` edit; PIP purchase-pricing note inside) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Tax Planning — Diron Insley display-only discount (member 59073)** (2026-07-14, `claude/vfo-session-setup-90d0eb`; SHIPPED backend v600 + `live-65-diron-tax-discount`; migration `20260714210000_tax_discount_applied`; retire via `constants/tax-discount.ts` + FE `DISCOUNT_MEMBER_NUMBER`; owed: delete locked orphan edge dir `client-migration-vfo-portal-8251b1`) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Background Check & Due Diligence Fee relabel + VFO Certified/Accredited search filter + test-data cleanup** (2026-07-14, `claude/vfo-session-setup-23b9de`; SHIPPED backend v598 + `live-64-bgdd-credential-filter`; owed: user cancels 2 leftover Stripe sandbox customers + deletes Gmail drafts) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Company Name/Website onboarding→profile + agreements named `-First-Last` in the ERT vault** (2026-07-14, `claude/vfo-session-setup-714aab`; SHIPPED backend v597 + frontend deployed; gotcha #221; owed: post-v597 smoke re-run, `live-63` tag stamp) — archived in [CHANGELOG.md](CHANGELOG.md).

- **New Model Sale team email — MSM dropdown + per-pipeline Skool line + Direct/Advisor label** (2026-07-14, `claude/heuristic-faraday-1cf0ca`; SHIPPED backend v596 + `live-62-newmodelsale-msm`) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Specialist profiles — one profile per person, multiple ecosystems (`experts.ecosystem_content` jsonb + `SpecialistsPanel.jsx` rewrite + Todd Lofgren/Darren Sugiyama row combines)** (2026-07-13, `claude/vfo-session-setup-14ec17`; no backend code; frontend since SHIPPED via the live-62..64 deploys; gotcha #220) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Accountant Onboarding — "Direct or Advisor Partnership" relabel + CC Connected Advisor** (2026-07-13, `claude/vfo-session-setup-8c102d`; backend v595 + frontend DEPLOYED; +2 actions → 408; gotcha #219) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Initial Contact "Who is completing the tracking?" VFOS/Member gate + Preliminary Setup removal** (2026-07-13, `claude/vfo-session-setup-1824cb`; backend v594; the 2 tracker files since SHIPPED via the live-62..64 deploys; gotcha #218) — archived in [CHANGELOG.md](CHANGELOG.md).

- **PFT "Undecided" client-decision flow + VFO Associate parity + AI PC Admin history + 90-Day-Plan profile-picture step** (2026-07-13, `claude/vfo-session-setup-4ff96a`; backend v593 + frontend `live-58`; +2 actions. **Still owed:** flip `PARTNERSHIP_FAST_TRACK` sandbox→live + the 3 `PFT_decision_*` templates Draft→Send before real accountants) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member Membership Fees — full billing system (Phases 1–3 of 5)** (2026-07-13, `claude/member-membership-fees`; backend v585→v591; cron 16; +9 actions; gotchas #215–#217; `/membership-pay` frontend since SHIPPED via the live-62..64 deploys. **Phase 4 reminder button DONE 2026-07-15 (v605 — see top bullet)**. Still owed: go-live flips — `MEMBER_MEMBERSHIP` SANDBOX→LIVE, confirm LIVE Stripe endpoint subscribes the checkout+PI events, 3 templates Draft→Send, verify `advisor_model` tags. Full model: [flows/membership-fees.md](flows/membership-fees.md)) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Admin home page Active counts aligned to KPI pages** (2026-07-10 third session, `claude/vfo-session-setup-ea76a1`; frontend-only `AdminPortal.jsx` welcome cards match the KPI pages' Active predicate — since shipped) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Growth Credits overhaul + admin Notifications page & personal reminders + notes internal/shared visibility + overview column sorting** (2026-07-10 second session, backend v579→v584, frontend SHIPPED `live-55-gc-notifications-notes` (react #115), +9 actions →395, crons 14/15, gotchas #210–#214 — ALL SHIPPED; owed: flip `GROWTH_CREDITS` sandbox→live) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Growth Plan — permanent priority numbers + every-change MSM bell + Owned/Assisted admin combo + "4 Ways" renames** (2026-07-10, `claude/vfo-session-setup-6bdcc3`; backend DEPLOYED v578→v579, frontend SHIPPED `live-54-growth-permanent-numbering` (react #114), `growth_plan_actions.plan_number` + 2 notification-rule migrations, gotchas #208–#209 — ALL SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Advisor/Accountant KPIs — separate-count carve-out + Suspended & Paused sub-tab** (2026-07-10, `claude/competent-sanderson-36ae8d` vfo-react ONLY; frontend SHIPPED `live-53-kpi-standing` react #113; NO backend, NO DB; `MemberKpiPanel.jsx` `SEPARATE_BUCKETS` carve-out — advisor excludes `VFO Reconciliation (Free)`, accountant excludes `Team Member` — + Suspended & Paused sub-tab + `stack` prop on `KpiKit SplitDonut`) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member & client profile restyle + member headshot/bio/website** (2026-07-10, `claude/profile-restyle`; backend DEPLOYED v577→v578, 3 `members` columns, gotcha #207; frontend SHIPPED `live-52-profile-restyle` — ALL SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **ERT/VFOS Documentation vault (3rd, read-only-for-owner section) + auto-copy signed agreements on sign+pay** (2026-07-09 late, `claude/ert-vfos-vault`; backend DEPLOYED v575→v577, +4 actions, 3 private buckets, gotchas #204–#206; frontend SHIPPED `live-51-ert-vault` — ALL SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Client Overview admin tab — per-program track overview + skipped-step warnings** (2026-07-09 evening, `claude/client-overview`; backend DEPLOYED v573→v575, +1 action `client_overview_load` →382, frontend SHIPPED `live-50-client-overview`, gotchas #202–#203 — ALL SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Specialist Revenue — house-account "View account activity in Stripe" deep link** (2026-07-09, `claude/specrev-stripe-account-link`; backend LIVE v573–v575, frontend SHIPPED `live-49-specrev-stripe-account-link` (react #108), new column `specialist_revenue_house_account.stripe_account_id`, gotcha #200 — ALL SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Specialist CSV reconciliation + profile heading polish + public-widget data hardening** (2026-07-09; DB data via MCP + `live-43-specialist-profile-headings` + anon column-grant hardening of `experts`, gotcha #201 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Specialist Revenue — recurring monthly payment plans** (2026-07-09, `claude/vfo-recurring-specialist-revenue-67e23f`; backend v563→v569, frontend SHIPPED `live-48-specrev-recurring` (react #107), +6 actions →381, gotchas #197–#199 — ALL SHIPPED; owed: MAP 1 sandbox→live flip + cancel leftover sandbox "Test Specialist" Stripe subs) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Sentry triage + license-fix review/hardening + exec-vote ordering fix + Sentry dev-gating** (2026-07-08, backend v561 + exec-vote fix shipped v563; Sentry gating merged react #106, since SHIPPED in the live-48+ deploys; gotchas #194–#196; user still owes: cancel test Stripe sandbox subscription(s) + mark the 9 Sentry issues Resolved) — archived in [CHANGELOG.md](CHANGELOG.md).

- **One Page Growth Plan — decimal sub-task numbering** (2026-07-08, frontend `live-47-growth-subtask-numbering` — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Growth Plan — Get Started/4-Ways/hub overhaul + accountant variant + notification fixes** (2026-07-08, backend v557→v560, frontend `live-46-growth-plan-4ways` (react #104), gotchas #191–#193 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Dead-code cleanup — retired the rotating per-request bank-transfer plumbing** (2026-07-07, backend v556, frontend `live-45-specrev-deadcode-cleanup`; gotchas #184–#185 SUPERSEDED, #189 clarified) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Specialist Revenue — shared house-account "record expected payment → mark received" model** (2026-07-07, backend v554→v555, frontend `live-44-specialist-revenue-house-account` (react #102 / edge #75), gotchas #189–#190 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Specialist $99/mo license invoice/receipt + Stage 4→5 automation fix** (2026-07-07, backend-only, deploys v545→v550 with save-point tag `backend-good-2026-07-07-v551`, gotchas #187–#188 — independently reviewed + hardened 2026-07-08 → v561; the claim RPC's migration file was committed 2026-07-08, gotcha #196) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Specialist Revenue — "Bank transfer" payment option** (2026-07-06, `live-40-specrev-bank-transfer`, gotchas #184–#185 — SHIPPED then SUPERSEDED the next day by the house-account model; its rotating per-request webhook/UI/columns/templates were removed 2026-07-07, v556) — archived in [CHANGELOG.md](CHANGELOG.md).

- **VFO Specialist Revenue — per-recipient "Transaction details" + pay-page redirect-loop fix** (2026-07-06, backend v543, `live-41-specrev-transaction-details`, gotcha #186 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Navigation/reload persistence + logo-home + tax Implementation-decision green + PFT visible-step counts** (2026-07-06, react #96, `live-39-nav-reload-persistence`, gotchas #182–#183 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Advisor/accountant Paused/Suspended standing labels** (react #95, `live-38-standing-labels` — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Email Templates command center — Draft/Send toggle, role-chip recipients, hardcoded-email ports** (2026-07-03/04, backend v540/`backend-good-2026-07-04-v540`, frontend `live-37-email-templates`, gotcha #181 — SHIPPED) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Notification audit + rule-driven Notification Editor + compact bell + BoldSign-declined/check-uncleared/tax-upload notifications** (branch `claude/eager-panini-fe804f`, backend v537→v538 + mark-read guard in v539 + `boldsign-webhook` v40, frontend `live-36-notification-editor`, gotchas #176–#180 — everything shipped; `notification_rules` table + `utils/notify.ts notifyByRule()` layer) — archived in [CHANGELOG.md](CHANGELOG.md).

- **90-Day-Plan sections + training videos (Wistia + Loom) + MAP 4 reminder ladder + Tax 3 green fix + Test Client reset** (2026-07-02, `live-33`/`live-34`/`live-35-tax-loom` + backend v536, gotchas #173–#175 — everything shipped) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Portal theme (light/dark) + skeleton rebuild + KPI overhaul + Lost/Removed login block** (branch `claude/modest-taussig-1bd4e1`, backend v535, gotchas #169–#172; theme/skeletons since merged + live) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Member Overview tab + per-admin tab-access grants + specialist-CIQ removal** (branch `claude/exciting-kare-ecf021`, v529→v534, gotchas #167–#168) — archived in [CHANGELOG.md](CHANGELOG.md).

- **Strategic Partner revenue-split feature + Tracy cross-check removed + Accounting Totals rows** (branch `claude/magical-hamilton-30df8b`, v529) — archived in [CHANGELOG.md](CHANGELOG.md).

_(Older sessions — Tracy client-paid rework + 3-vault unification [v525], specialist ecosystem edits [v523], License Fee Continuation + Accounting build-out [v522], VFO Specialist Revenue [v513], Strategic Members [v505], Advisor/Accountant KPIs, Client Payment-Continuation [v503], security & resilience remediation [v496], and earlier — archived in [CHANGELOG.md](CHANGELOG.md).)_

- **`boldsign-webhook`**: modified with tax + advisor + accountant routing + advisor & accountant `_stripecustomer` + `_ceocountersign` chains (all with prior user approval). Deploys MUST use `--no-verify-jwt` flag. Future edits still require explicit approval.
- **Rollback**: full version history available in Supabase Dashboard → Edge Functions
- **`deno check` baseline**: **0 errors** as of 2026-05-29. The historical 7 (4 `existing-null` in `actions/pipeline/pipfu-decision.ts`; 3 `pipeRow null` in `router/webhooks.ts`) were resolved with explicit null-guards — `if (!existing) return 500` after the upsert-reassign branch in pipfu-decision, and a `pipeRow &&` narrow on the `payment_intent.succeeded` quarterly P2–P4 block in webhooks.ts. See gotcha #45. Going-forward gate: any error = fail.
- **`boldsign-webhook` deploys MUST use `--no-verify-jwt` flag.** `config.toml` has `verify_jwt = false` for stickiness, but always pass the flag — live registry setting is what matters.
- **Crons**: Fifteen jobs via `pg_cron` + `pg_net` — the twelve daily sweeps below PLUS (added 2026-07-13) **`membership-sweep-daily` jobid 16 @12:00 UTC** (`automation_MEMBERSHIP_sweep` — member membership fees: renewals → waive $0 rows → one combined off-session charge per plan for due+missed months → auto-unsuspend; doc `supabase/cron/membership-sweep.sql`; see [flows/membership-fees.md](flows/membership-fees.md)) PLUS (added 2026-07-10) **`reminder-sweep-5min` jobid 14** (`*/5 * * * *`, `automation_REMINDER_sweep` — delivers due `personal_reminders` as bell rows via a DIRECT `notifications` insert, the documented #176 exception) and **`notifications-purge-daily` jobid 15 @10:30 UTC** (`automation_NOTIFICATIONS_purge` — permanently deletes READ notifications older than 90 days; unread never touched). Docs: `supabase/cron/reminder-sweep.sql` + `notifications-purge.sql`; both installed by cloning an existing job's command in-SQL so the key never left Postgres. The daily twelve (also `regular-map4-followup-sweep-daily` jobid 11 @09:30 — the Regular-Priorities MAP 4 **3-tier ladder**: follow-up email (meeting+2d) → reminder email (follow-up_sent+2d, `REGULAR_map4reminder`) → stall bell to Tracy + assigned PF (reminder_sent+2d), each gated by `map4_form_submitted_at IS NULL`; gotcha #175 — + `growth-overdue-sweep-daily` jobid 12 @10:00): `revshare-sweep-daily` (MAP1) at **02:00 UTC** (also runs the MAP 1 48h/96h reminder ladder on three stalls: PCADMIN Undecided email, agreement signing, Pay1 link), `tax-revshare-sweep-daily` at **02:30 UTC** (also runs Tax 4 timer Continue-vs-Stop logic, Tax 5 implementation-decision timers, Tax 3 reminder timers, Tax 4 meeting-date nudge to Tim), `chargescheduled-sweep-daily` at **03:00 UTC**, `check-reminder-sweep-daily` at **04:00 UTC** (7-day pre-due check nudges + NEW 2026-07-03: the uncleared-check bells `MAP1/TAX_check_uncleared_bell` — check recorded/due but never marked cleared after 14d editable, incl. overdue quarterly P2-P4), `advisor-sweep-daily` at **05:00 UTC** (3 stalls × 48h reminder + 96h PF notification on advisor onboarding + 14-day implicit-No auto-decline that writes `final_decision='Auto-Declined'` and chains `automation_ADVISOR_declineemail`), `accountant-sweep-daily` at **06:00 UTC** (same 3-stall + 14-day pattern as advisor, against `accountant_onboarding`, chains `automation_ACCOUNTANT_declineemail` on auto-decline), `specialist-sweep-daily` at **07:00 UTC** (`automation_SPECIALIST_sweep` — **7 stalls** × 48h reminder email + 96h Tracy FYI: SIF not submitted, exec hasn't voted R1/R2, Core/Max/Questions not chosen, DD checklist not submitted, final rev-share unanswered, agreement not signed, license payment not made; NO 14-day auto-decline; doc `supabase/cron/specialist-sweep.sql`; installed by copying the advisor job's Authorization header in-SQL so the key never left Postgres), `pft-sweep-daily` at **08:00 UTC** (`automation_PFT_sweep` — Partnership Fast Track: discovery-form 2-day reminder email to the accountant + 4-day PF notice, VFO Fast Track email 2-day reminder email + 4-day PF notice, and (2026-07-13) **Undecided-decision 2-day reminder email (re-sends `PFT_decision_undecided`) + 4-day PF notice**; timers on `pft_engagement`; NO auto-decline; installed by cloning the specialist job's command in-SQL), `tax-presentation-sweep-daily` at **09:00 UTC** (jobid 10, `automation_TAX_presentation_sweep` — drafts the `TAX_presentation_link` email To member / Cc assigned PF for any `client_tax_plans` row whose `presentation_send_date <= today` and `presentation_email_sent_at IS NULL`; **drafts only, no auto-send** — convention-consistent; installed via `cron.schedule` with the same Bearer pattern as the other jobs), `specialist-revenue-payout-sweep-daily` at **11:00 UTC** (jobid 13, `specialist_revenue_payout_sweep` — retries pending/awaiting-Connect/failed VFO Specialist Revenue payouts + the 48h reminder / 96h Tracy-FYI ladder; doc `supabase/cron/specialist-revenue-payout-sweep.sql`; installed by cloning the advisor job's command in-SQL). Staggered to avoid races on the same row. **PIP Meetings has no cron** — purchases are admin-driven and there's no client-side wait worth chasing; stalled/failed PIP chains need manual re-fire via service-role.
- **Sweep timing is rule-driven (2026-07-03):** every reminder-email / stall-bell tier in the daily sweeps reads its delay-days + on/off from 
otification_rules via getRuleConfig() (defaults 2d email / 4d bell); the 24h auto-locks and 14-day auto-declines stay hardcoded (business actions, not notifications). Edit in Automation -> Notification Editor. Gotcha #176.
- **Stripe env vars**: ⚠️ **LIVE mode = a REAL Stripe account (real money).** The non-sandbox keys (`STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET`) point at a **live Stripe account** — switching any pipeline to LIVE and completing a checkout **charges a real card**. Only the sandbox keys (`STRIPE_SECRET_KEY_SANDBOX` + `STRIPE_WEBHOOK_SECRET_SANDBOX`) hit the test account. (Corrected 2026-06-01 — the prior "both modes = VFO Services Sandbox account" claim was wrong; a $1 MAP 1 live test actually charged a real card and had to be manually refunded in the Stripe dashboard.) The webhook handler tries both signing secrets so either mode validates. MAP 1 has no built-in P1 refund — refund live test charges via the Stripe dashboard.
- **Sandbox mode**: Toggleable from the header badge of **all five** automation panels — MAP 1 (`AutomationPanel`), Tax (`TaxAutomationPanel`), and (added 2026-06-01, **frontend dev-only/uncommitted**) Advisor, Accountant, and PIP via the shared `components/admin/SandboxModeToggle.jsx` (clickable badge + confirm modal). PIP's toggle flips the `'MAP 1'` row (shared). Admin-only. `save_sandbox_config` accepts a `pipeline` parameter (defaults to `MAP 1` for backward compat). All rows flip `sandbox_mode` + `stripe_test_mode` + `boldsign_test_mode` together. `pipeline_sandbox_config` must have rows for every pipeline that uses sandbox-aware handlers; the TAX row is required for tax automation to work in sandbox; the ADVISOR_ONBOARDING row is required for advisor onboarding to work in sandbox; the ACCOUNTANT_ONBOARDING row is required for accountant onboarding to work in sandbox; the **SPECIALIST_ONBOARDING row** (added 2026-06-03) is required for specialist background-check Stripe payments to work in sandbox — it has a working toggle in the new `SpecialistAutomationPanel` and is **currently `sandbox_mode=true` for testing (flip to live before any real specialist payment)**. **PIP Meetings reuses the `'MAP 1'` row** intentionally (PIP rides on Holistic Planning's sandbox toggle, so the master MAP 1 flag controls PIP too — no separate `'PIP'` row needed). **The `GROWTH_CREDITS` row** (added 2026-07-10) drives member Growth Credit purchases (`gc_create_checkout`); its toggle lives in **Automation & Config → Growth Credits** and is **currently `sandbox_mode=true` for testing (flip to live before any real credit sale)**. **The `MEMBER_MEMBERSHIP` row** (added 2026-07-13) drives member membership-fee setup links + monthly pulls; its toggle is the badge on both Accounting → Members → Advisor/Accountant Membership Fees panels and is **currently `sandbox_mode=true` (flip to live before real membership billing)**.
- **Frontend UX layer** (live as of 2026-05-27): `src/lib/api.js` is **113 lines** and exports the original `callApi` / `getSession` / `setSession` / `clearSession` PLUS a module-level promise cache: `loadCachedData()` (caches `load_data`), `loadCachedAction(action, payload)` (keyed cache for other reads — used for `msm_load_programs`, `msm_load_training_track`, `msm_load_client_track`), `clearCachedData()`, `clearActionCache()`. Cache invalidates on `clearSession`. `callApi` enforces a **20-second `AbortController` timeout**; reads matching `^load_|_load(_|$)` (plus explicit `vault_list`) auto-retry once on timeout, writes never retry on timeout (prevents double-writes). The **Skeleton library** lives in the `src/components/shared/skeletons/` package (rebuilt 2026-07-02): `primitives.jsx` (Skeleton bar, SkeletonRow/Card/Stats, CardShell, HeroSkeleton, ListHeader/SearchFilter/Table/TokenForm skeletons) composed into page skeletons grouped by area — `admin.jsx` (DirectoryList, AutomationTracker, AccountingTable, EmailTemplates, MemberOverview, StrategicPartners, Onboarding list/detail, MemberProfileDetails, AdminMsmHome, AdminProgramView, ProgramNotes), `tracks.jsx` (Map1/PFT/Training tracks, TaxPlanList, PIP list/detail, PhaseList, ClientsList, CoachingMeetings/Renewal, MsmHome, CiqList), `portal.jsx` (Showroom, VaultSections/VaultRows, SharedDocs, MemberProfile). `Skeleton.jsx` re-exports the whole package for back-compat — import from either path. Every loading state in the app renders a page-shaped skeleton (no "Loading…" text anywhere); list skeletons show 2–3 rows; all colors are `var(--vfo-*)` so they theme in dark mode. Shimmer keyframes live in `src/index.css` (`.vfo-skeleton` + `vfo-skeleton-shimmer`). **Theming:** `src/lib/theme.js` (`vfo_theme` localStorage key, `usePortalTheme()` in the five portal-level pages, `applyThemeForCurrentRoute()` at boot) — see gotcha #169.

### Handler / cron catalog

The per-handler "recent post-refactor additions" list (the `automation_CONTRACT_*` sweeps + `paidbycheck` / `checkcleared`, `save_sandbox_config`, and the Tax / Advisor / Accountant / PIP automation handler groups) is **fully cataloged in the canonical docs** — see [architecture/05-api-action-catalog.md](architecture/05-api-action-catalog.md) (every action + its file), [architecture/01-system-map.md](architecture/01-system-map.md) (the nine daily `pg_cron` jobs with times + purposes), and the per-pipeline [flows/](flows/) docs. The sweep/cron behaviours are also summarized in **Stripe webhook chain order** immediately below. _Verbatim original archived in [CHANGELOG.md](CHANGELOG.md)._

### Stripe webhook chain order (current)

After `_invoicereceipt` in all three MAP1 chain sites (MAP1 first-card, quarterly N, ACH cleared), the handler also chains `automation_CONTRACT_revshare`, which now **pays the share immediately** — the Tracy Revenue-Master cross-check was removed 2026-07-01 (gotcha #164), so there is no more `pending` bailout; amounts come straight from the PF input form on `pipeline_map1`. The daily revshare sweep now only retries **failed** transfers (and auto-completes any legacy `Pending` rows). This chain also transfers the 10% **strategic partner share** to `strategic_member_groups.stripe_account_id` when the connected member is a strategic member, and drafts the partner rev-share email.

**Tax payments** are routed inside the same Stripe webhook handler — after the MAP1 `pipeline_map1` lookup misses, the handler falls back to `client_tax_plans` by `stripe_customer_id` AND branches on `metadata.payment_kind`:
- `retainer` (Tax 3 first checkout) → chains `automation_TAX_confirmationemail` (always) + `automation_TAX_invoicereceipt` (card only on first event; ACH waits for `payment_intent.succeeded`). **No revshare chain on payment** — tax revshare for the retainer is triggered later by EITHER (a) the client clicking the green "Continue now" button on the Tax 4 Continue email (writes `post_review_client_decision='Confirmed'`, fires `automation_TAX_revshare` immediately via `automation_TAX_postreview-client-decision`), OR (b) the 24h auto-lock in the sweep when no client click happens (writes `post_review_client_decision='Auto-Locked'` and fires `automation_TAX_revshare`). The Tax 4 Continue client email has TWO buttons: green "Continue now" (skip 24h grace) + red "Refund my retainer" (back out). For Undecided admin picks the email has 2 buttons (Proceed → revshare; Refund → refund), and the client click fires the chain immediately via `automation_TAX_postreview-client-decision`.
- `implementation` (Tax 5 off-session charge initiated by client clicking green "Proceed now" on the Tax 5 Proceed email — writes `implementation_final_decision='Confirmed'` — OR by 24h Proceed-by-default sweep auto-lock; both fire `automation_TAX_charge-implementation`) → chains `automation_TAX_implementation-receipt` (REC PDF + Gmail draft). No revshare chain on payment (implementation revshare picked up by the next sweep tick once `implementation_receipt_number` is set).
**Advisor onboarding payments** are routed inside the same Stripe webhook handler — after the MAP1 + tax lookups miss, the handler falls back to `advisor_onboarding` by `stripe_customer_id` AND branches on `metadata.pipeline='ADVISOR_ONBOARDING'` + `metadata.payment_kind='onboarding'`. On `checkout.session.completed`: writes `payment_status` ('succeeded' for card, 'processing' for ACH), `payment_method_type`, `acct_last4`, `card_processing_fee`, `stripe_payment_intent_id`, `payment_completed_at` (card path), `renewal_date` (today + 6 months). For card, immediately chains `automation_ADVISOR_confirmationemail` (which chains `automation_ADVISOR_invoicereceipt`). For ACH, the eventual `payment_intent.succeeded` flips `payment_status='succeeded'` + writes `renewal_date` and chains the same. **No revshare chain on advisor payment** — advisor onboarding-created members are Money Mapping by default; VFO holds the share internally.

**Accountant onboarding payments** are routed inside the same Stripe webhook handler — after the MAP1 + tax + advisor + PIP lookups miss, the handler falls back to `accountant_onboarding` by `stripe_customer_id` AND branches on `metadata.pipeline='ACCOUNTANT_ONBOARDING'` + `metadata.payment_kind='onboarding'`. On `checkout.session.completed`: writes `payment_status`, `payment_method_type`, `acct_last4`, `card_processing_fee`, `stripe_payment_intent_id`, `payment_completed_at` (card path), `renewal_date` (today + 6 months). For card, immediately chains `automation_ACCOUNTANT_confirmationemail` (which chains `automation_ACCOUNTANT_invoicereceipt`). For ACH, the eventual `payment_intent.succeeded` flips `payment_status='succeeded'` + writes `renewal_date` and chains the same. **No revshare chain on accountant payment** — accountant onboarding-created members have no revenue_decision; VFO holds the share internally.

**PIP Meetings purchases** are routed inside the same Stripe webhook handler — after the MAP1 + tax + advisor lookups miss, the handler falls back to `client_priority_tracks` by `pip_stripe_customer_id` AND branches on `metadata.pipeline='PIP'` + `metadata.payment_kind='purchase'`. On `checkout.session.completed`: writes `pip_payment_status` ('succeeded' for card, 'processing' for ACH), `pip_payment_method_type`, `pip_acct_last4`, `pip_card_processing_fee`, `pip_payment_intent_id`, `pip_payment_completed_at` (card only). For card, immediately chains `automation_PIP_confirmationemail` + `automation_PIP_invoicereceipt` + `automation_PIP_revshare`. For ACH, the eventual `payment_intent.succeeded` (gated by `pi.metadata.pipeline === "PIP"`) flips `pip_payment_status='succeeded'` + writes `pip_payment_completed_at` and chains the same three handlers. **PIP revshare unlocks locked child meetings** — on Additional PIP success, `automation_PIP_revshare` flips `pip_paid=true` on every `client_priority_tracks` row where `pip_purchased_from_track_id` matches the source meeting.

**Specialist Onboarding payments** route inside the same handler. **Background check** (Stage 3): after the MAP1/tax/advisor/PIP/accountant lookups, falls back to `specialist_onboarding` by `bg_stripe_customer_id` + `metadata.payment_kind='background_check'` → `checkout.session.completed` writes `bg_*` + chains `automation_SPECIALIST_bgconfirmation` (both methods) + `automation_SPECIALIST_bgreceipt` (card now / ACH on `payment_intent.succeeded`). **Monthly license** (Stage 4, **`mode=subscription`**, `metadata.payment_kind='license'`) is handled by **three separate additive blocks** (NOT the `stripe_customer_id` cascade): (a) `checkout.session.completed` (subscription) records `lic_subscription_id`/customer/method + marks `payment_made` + chains `automation_SPECIALIST_licconfirmation`; (b) **`invoice.paid`/`invoice.payment_succeeded`** routed by `lic_stripe_customer_id` (subscription ref via `invoice.parent.subscription_details.subscription`, gotcha #76) — captures the payment method if not yet set (gotcha #77), per-invoice idempotent on `lic_last_invoice_id`, **first** invoice (`billing_reason=subscription_create`) → `licconfirmation` (idempotent) + `automation_SPECIALIST_licinvoicereceipt` (`INV-SPECLIC`/`REC-SPECLIC` PDFs) + advance **Stage 4→5**, **recurring** → monthly invoice/receipt only; (c) **`invoice.payment_failed`** → `lic_payment_status='failed'` + Tracy dunning FYI. The license customer **reuses `bg_stripe_customer_id`** (`lic_stripe_customer_id === bg_stripe_customer_id`); the BG cascade branch harmlessly skips a license event because `bg_payment_status='succeeded'` by Stage 4. **`boldsign-webhook`** gained a `specialist_onboarding` branch (keyed on `lic_boldsign_document_id`) that chains `automation_SPECIALIST_ceocountersign` (specialist signed) and `automation_SPECIALIST_licstripecustomer` (Completed).

**Recurring Specialist Revenue plans (2026-07-09, v569)** are handled by their own additive blocks, ALL routed by each plan's DEDICATED Stripe customer (`specialist_revenue_recurring_plans.stripe_customer_id` — plans never share a customer with anything else, so there is zero collision with the license routing or the customer-id cascade): `checkout.session.completed` (`mode=subscription` + `metadata.pipeline='VFO_SPECIALIST_REVENUE_RECURRING'`) flips the plan `setup_pending→active`, captures ACH last4 + `next_billing_date` (derived `trial_end || current_period_end || items.data[0].current_period_end || billing_cycle_anchor` — gotcha #199) and chains the one-time `specialist_revenue_recurring_confirmationemail`; **`invoice.finalized`** (NEW event — must be subscribed on the endpoint, both modes; added 2026-07-09) pre-creates the month's `specialist_revenue_requests` row as `processing` + copies the plan's lines (skips the $0 `subscription_create` stub — gotcha #198; idempotent via the `stripe_invoice_id` unique index); the invoice-paid events funnel through `processSpecrevRecurringInvoicePaid` (subscription-match guard + atomic `claim_specrev_recurring_invoice` RPC + find-or-create by `stripe_invoice_id`, self-healing if finalized never arrived) → row `received` → chains the EXISTING `specialist_revenue_invoicereceipt` + `specialist_revenue_payout`; `invoice.payment_failed` → row `failed` + `SPECREV_recurring_payment_failed_bell` (plan stays active — Stripe retries); `customer.subscription.updated/deleted` terminal end → plan `canceled`. Admin cancel (`specialist_revenue_recurring_cancel`) deletes the Stripe subscription immediately; an in-flight monthly ACH still settles and pays out.

**Failure-event handling (added 2026-06-15, v464/v465 — routes every money-movement failure to Jake's bell via `utils/notify-jake-failure.ts`, in ADDITION to existing Tracy/admin/PF alerts):** `checkout.session.async_payment_failed` (ACH first-payment bounce, all pipelines) and the broadened `payment_intent.payment_failed` (non-Specialist first payments) resolve the row via `utils/resolve-stripe-failure.ts` and flip its first-payment status to `failed`. **As of v612** `payment_intent.payment_failed` ALSO handles LATE-ACH failures of OFF-SESSION charges (previously skipped) via two additive branches — MAP1 installment (`metadata.payment_number` 2-4, acts only when `pay{N}_status==='processing'`) and TAX implementation (`metadata.payment_kind='implementation'`, acts only when `implementation_charge_status==='processing'`) — each flipping to `declined` + firing its `*_charge_failed` rule + `notifyJakeFailure`; the `'processing'` guard prevents double-alerting a card sync-decline the sweep already handled (gotcha #229). `customer.subscription.updated`/`deleted` set `lic_payment_status` past_due/canceled (license lapse → revoke-access alert; auto-clears on return to active, routed by `lic_subscription_id`). `charge.dispute.created`/`closed` (chargebacks — close clears the opened alert, then posts won/lost so it stays visible), `charge.refunded` + `charge.refund.updated`/`refund.updated`/`refund.failed` (refund tracking incl. Dashboard-issued + failed-refund alert), and `transfer.reversed` (rev-share clawback) round it out. A catch-all `console.log("Stripe webhook event:", type)` logs every event. **`payment_intent.partially_funded`** (added 2026-07-06, VFO Specialist Revenue bank-transfer only) is an additive block → dismissible FYI bell to Jake (`SPECREV_partial_funding_bell`) + persists received-so-far dollars to `specialist_revenue_requests.bank_transfer_amount_received`; the received-so-far total is derived as `amount - next_action.display_bank_transfer_instructions.amount_remaining` because a `customer_balance` PI reports `amount_received=0` until fully funded (gotcha #184). The full-funding `payment_intent.succeeded` for a bank-transfer request (guard widened to accept `payment_status='requested'` + bank_transfer) flips it to `received` and chains invoice/receipt + member payout. **Alert policy:** action-required + auto-clear for rev-share/license/disputes (clean recovery event); dismissible FYI for the rest (no programmatic recovery → would be permanent clutter — gotcha #122). These handlers only fire if the Stripe endpoint subscribes to the events (config, both modes).

The chargescheduled-sweep creates off-session PaymentIntents with `metadata.payment_number=N`; the resulting `payment_intent.succeeded` webhook flips `payN_status` and chains the usual downstream. On Stripe failures (declined / auth_required), the sweep sets `payN_status` to a failure value and drafts a Gmail asking the client to use the `/pay` link. **As of v612:** the idempotency key is LOGICAL/date-less (`chargescheduled-<client_id>-P<n>`) so a re-select within Stripe's 24h window replays the SAME PaymentIntent instead of double-charging (gotcha #228); the failure email is drafted (via the shared `utils/map1-installment-failure.ts` `draftMap1InstallmentFailureEmail`) ONLY when a `checkout_token` exists — migrated rows now get one minted by `migration_backfill_map1` — and the bell text is honest ("manual follow-up required") when it does not; and the post-charge status write is error-checked, alerting Jake when the charge succeeded in Stripe but the row write failed. A LATE ACH bounce of an off-session installment (status `'processing'`) is now caught by `payment_intent.payment_failed` (gotcha #229), not just the synchronous sweep path.

The tax-revshare-sweep (02:30 UTC) does quintuple duty: (1) **retries** tax revshare (both retainer and implementation kinds) on plans where revshare was already kicked off (`Pending` or `Failed`) but couldn't complete — as of 2026-07-01 the Tracy Revenue-Master cross-check is removed (gotcha #164), so "pending" no longer means "sheet not populated"; the retry is now purely for **failed transfers** (+ a dedicated pass that retries a stranded strategic-partner transfer and drafts its partner email). **Retry-only**: the sweep does NOT auto-start revshare on plans that haven't reached the Tax 4 admin decision yet (this was a latent bug; fresh plans must reach the Tax 4 Continue 24h auto-lock OR an explicit Undecided→Proceed/Confirmed client click to set `Pending` first); (2) evaluates Tax 4 post-review timers — **24h auto-lock for Continue picks (writes `post_review_client_decision='Auto-Locked'` and fires `automation_TAX_revshare` directly)**, 48h reminder email for Undecided picks, 96h PF-notification for Undecided picks; (3) evaluates Tax 5 implementation-decision timers — **24h auto-charge for Proceed picks (writes `implementation_final_decision='Auto-Locked'` and fires `automation_TAX_charge-implementation`)**, 48h reminder email for Undecided picks, 96h PF-notification for Undecided picks; (4) evaluates Tax 3 reminder timers — **48h reminder + 96h PF-notification** on each of three stalls (Undecided email not yet clicked, agreement not yet signed, payment link not yet acted on). **Tax 3 reminders are 48h/96h only — there is no 24h tier on Tax 3; the 24h is exclusively the Tax 4/5 auto-lock.** (5) drafts the Tax 4 meeting-date nudge to Tim Gacsy when `tax4_meeting_date` is set, has passed, and no Tax 4 decision is recorded — one draft per UTC day per plan.

The MAP1-revshare-sweep (02:00 UTC) does dual duty: (1) **retries** MAP 1 revshare on `rec{N}_number IS NOT NULL AND rev_paid NOT IN ('Yes','Money Mapping','N/A — No Share Due')` rows (includes previously-`Failed` transfers so misconfigured Connect accounts auto-recover); (2) evaluates the MAP 1 48h/96h reminder ladder for three stalls — PCADMIN Undecided email (`c14_email_sent_at` timer, only written on Undecided branch), agreement signing (`c17_followup_sent_date` timer, written by `_sendagreement`), Pay1 link (`pay1_email_sent_at` timer, written by `_paymentemail`). Each stall has independent 48h and 96h queries with `*_reminder_sent_at` / `*_pf_notified_at` idempotency guards. Reminder emails are To-client-only (matching tax pattern). 96h PF notifications insert `notifications` rows with `pipeline='MAP 1'`, `link='/admin/client/<id>?tab=map1'`.

The advisor-sweep (05:00 UTC) does pure reminder-ladder work plus the 14-day implicit-No rule — there's no revshare retry (advisor onboarding is Money Mapping by default, no Stripe Connect transfer to retry). Three stalls × 48h reminder + 96h PF notification: Undecided email (`decision_email_sent_at` timer, written by `automation_ADVISOR_decision` Undecided branch), agreement signing (`agreement_sent_at` timer, written by `_sendagreement`), payment link (`payment_link_sent_at` timer, written by `_paymentemail`). Idempotency guards on `advisor_onboarding`: `decision_reminder_sent_at` / `decision_pf_notified_at`, `signing_reminder_sent_at` / `signing_pf_notified_at`, `payment_reminder_sent_at` / `payment_pf_notified_at`. Reminder emails are To-advisor-only. 96h PF notifications insert `notifications` rows with `pipeline='ADVISOR_ONBOARDING'`, `link='/admin?tab=advisors&section=advisor_onboarding'`. Plus a 14-day branch on Undecided: if `decision_email_sent_at` is more than 14 days ago and `final_decision IS NULL`, sets `final_decision='Auto-Declined'` + `final_decision_at=now()` and chains `automation_ADVISOR_declineemail`. **PostgREST gotcha**: the payment-stall query uses `.or('payment_status.is.null,payment_status.neq.succeeded')` because `.neq('succeeded')` alone excludes NULL rows (the common case for unpaid advisors).

**Note (ACH → implementation manual-repay):** if the retainer was paid via ACH, Stripe rejects the off-session implementation charge with `us_bank_account not allowed for this PaymentIntent`. The `automation_TAX_charge-implementation` handler degrades gracefully: sets `implementation_charge_status='declined'`, regenerates `checkout_token`, inserts an admin notification with the Stripe error, drafts a Gmail to the client with a fresh `/tax-pay` link. The page + `automation_TAX_stripecheckout` already branch on plan state to charge the implementation amount instead of the retainer. Client pays manually via `/tax-pay`; webhook chain routes by `metadata.payment_kind='implementation'`. Documented in [docs/flows/tax-planning.md](C:/vfo-react/docs/flows/tax-planning.md) Failure mode #9. **As of v612:** a LATE ACH bounce of an already-created off-session implementation charge (status `'processing'`) is now caught by the `payment_intent.payment_failed` handler, which flips `implementation_charge_status='declined'` + fires `TAX_impl_charge_failed` / `FAILURE_tax_implementation_charge` alerts (status + alerts only, no email — admin re-sends via Tax 5); gotcha #229.

The check-payment paths run entirely admin-driven (`_paidbycheck` → `_checkcleared` for MAP1 P1..P4; `automation_TAX_paidbycheck` → `automation_TAX_checkcleared` for tax retainer); no Stripe webhook involved.

The Setup-phase deposit refund path is also entirely admin-driven (`tax_save_deposit_pi` captures the Stripe PI on the Deposit Paid task, then if Tax Plan Greenlight = Stop the "Send refund" button fires `automation_TAX_depositrefund` which fetches the PI amount from Stripe, fires a full `/v1/refunds`, writes `deposit_refund_*` columns, drafts a Gmail confirmation, and inserts an admin notification). Program_id=4 only — Holistic Tax Priorities (program_id=1) clients arrive via MAP1 and skip the Setup phase.

---

## REQUIRED READING *(in order, before any work)*

1. `C:/vfo-react/docs/README.md` (doc index)
2. `C:/vfo-react/docs/architecture/01-system-map.md`
3. `C:/vfo-react/docs/architecture/03-edge-functions.md` — backend file layout
4. `C:/vfo-react/docs/architecture/05-api-action-catalog.md` — every action + its file
5. Whichever is relevant to the task:
   - `C:/vfo-react/docs/architecture/02-frontend-shell.md` (routes, pages, components — incl. /pip-pay, PIP Meetings tab on Holistic clients, "Loading UX" paragraph for skeleton library and module-level cache)
   - `C:/vfo-react/docs/architecture/04-auth-and-sessions.md`
   - `C:/vfo-react/docs/architecture/06-orchestration-files.md` (api.js entry updated for cache + timeout semantics)
   - `C:/vfo-react/docs/flows/*` (end-to-end flows incl. `tax-planning.md` and `pip-meetings.md`)
   - `C:/vfo-react/docs/integrations/*` (per-integration deep-dives)
   - `C:/vfo-react/docs/tables/*` (schema-by-domain incl. `tax.md`, `notifications.md`, `members.md`)
   - `C:/vfo-react/docs/glossary.md`
   - `C:/vfo-react/ADVISOR_ONBOARDING_RESUMPTION.md` (advisor onboarding state-of-the-work)
Treat docs as source of truth for architecture; verify against actual code before changing anything (docs can drift on details).

---

## DEVELOPMENT ENVIRONMENT *(Windows + PowerShell)*

- Deno (not on PATH): `C:\Users\jakel_fjetgbx\.deno\bin\deno.exe`
- Supabase CLI: `C:\Users\jakel_fjetgbx\scoop\shims\supabase.exe`
- Local Supabase stack: `supabase start` (Docker Desktop required)
- Local Postgres: `127.0.0.1:54322`, postgres/postgres
- Local function URL: `http://127.0.0.1:54321/functions/v1/vfo-admin-api`
- Studio: `http://127.0.0.1:54323`
- Dev admin login: `dev@local.test` / `dev2026` (admin role, NOT superadmin)
- Dev member login: `member@local.test` / `member2026` / member_number `TEST-001` (999 GC balance seeded)
- Frontend dev (prod backend): `cd C:\vfo-react; npm run dev`
- Frontend dev (local backend): `$env:VITE_API_URL = "http://127.0.0.1:54321/functions/v1/vfo-admin-api"; npm run dev`
- Local function-serve: `supabase functions serve vfo-admin-api --no-verify-jwt --env-file ./supabase/.env.local`
- Frontend deploy: `npm run deploy` (vite build + gh-pages) — **production deploy, requires explicit user approval**
- Backend deploy `vfo-admin-api`: `supabase functions deploy vfo-admin-api` (only with explicit user approval)
- Backend deploy `boldsign-webhook`: `supabase functions deploy boldsign-webhook --no-verify-jwt` (flag mandatory; only with explicit user approval)
---

## ENVIRONMENT GOTCHAS

- **Bash is NOT on PATH** — use PowerShell. `git`, `curl`, `docker`, `psql` all run via PowerShell or `docker exec`.
- **PowerShell does NOT have `tail`** — use `Select-Object -Last N` instead. `| tail -3` will silently break pipelines (the upstream command may not run at all, swallowing output).
- **PowerShell `sort -V` is NOT version-aware** — `sort` aliases `Sort-Object`, which returns LEXICAL order (`live-9` sorts after `live-64`). Derive max tags with `git tag -l 'live-*' --sort=v:refname | Select-Object -Last 1` (gotcha #222).
- **PowerShell mangles JSON inline** — use `--data-binary @file`:
  ```
  '{"action":"foo","x":"y"}' | Out-File tmp.json -Encoding ascii -NoNewline
  curl.exe -s -X POST <url> -H 'Content-Type: application/json' --data-binary "@tmp.json"
  Remove-Item tmp.json
  ```
- **PowerShell `Set-Content -Encoding utf8` AND `Out-File -Encoding utf8` both write a UTF-8 BOM** which leaks into commit message subjects (rendered as `﻿` in `git log`) and corrupts box-drawing characters. For bulk file writes AND commit messages:
  ```powershell
  $utf8 = New-Object System.Text.UTF8Encoding $false
  [System.IO.File]::WriteAllText($file, $content, $utf8)
  ```
- The Edit tool handles UTF-8 correctly — prefer it for moderate edits.
- **PowerShell here-string commit messages** can break on quoted text — use `git commit -F <tmpfile>` instead of inline `git commit -m`. AND write the tmpfile BOM-free per the above. The Bash tool's `cat > file << 'EOF'` heredoc avoids this entirely if you're already in a Bash context.
- **`git log --format=%B` in PowerShell collapses newlines into spaces** when piped through a variable — multi-line commit bodies become one long line. If you need to round-trip a message (e.g. to amend), rewrite the original from your prepared text rather than reading it back.
- **Supabase CLI stderr noise** (Bundling Function: …, Setting up Edge Functions runtime…) is benign — PowerShell flags it as RemoteException but it's normal CLI progress.
- **`vite build` stderr noise** (the `[plugin builtin:vite-reporter]` chunk-size warning) is similarly benign — PowerShell flags as RemoteException but the build succeeded. Look for `Published` at the end of `npm run deploy` output to confirm gh-pages push worked.
- **Local DB seed is sparse** — members table for TEST-001 doesn't exist; clients/pipeline_map1/email_templates start empty. Many "save didn't persist" reports are FK-driven seed gaps, not refactor regressions. See `supabase/seed.sql` for what IS seeded.
- **Local DB sequence gap** — `experts.id` has no DEFAULT in the pg_dump baseline; inserts via `save_specialist` against the local DB fail with not-null violation. Pre-existing test-data issue. (Frontend works around it by computing id client-side.)
- **CORS in local-dev** — Kong gateway intercepts OPTIONS preflight and stamps `Access-Control-Allow-Origin: *`. Production hits the function directly. Don't worry if local CORS smoke shows `*`.
- **`supabase/.temp/cli-latest` shows as modified after any CLI deploy** — Supabase CLI version-tracking artifact, not project code. Exclude from every commit.
- **GitHub Pages CDN + browser HTML cache** — after `npm run deploy` succeeds (look for `Published`), the live bundle filenames change (e.g. `index-<hash>.js`), but browsers often serve a cached `index.html` that points at the OLD bundle. Confirm the deploy reached the CDN by `curl -s https://vfoportal.com/ | grep -oE 'index-[A-Za-z0-9_-]+\.(js|css)'` and comparing to the new filename in the build output. If the live HTML serves the new bundle but the user's browser still shows old content, it's local cache — DevTools → Network → "Disable cache" → reload, or use incognito.
---

## VERIFICATION GATE *(run after non-trivial backend changes)*


```powershell
$denoExe = "C:\Users\jakel_fjetgbx\.deno\bin\deno.exe"
cd C:\vfo-edge-functions  # or worktree path
& $denoExe check supabase/functions/vfo-admin-api/index.ts 2>&1 | Select-String "TS|Found"
```

Pass = **0 errors**. Any error = fail. (Baseline was 7 until 2026-05-29; cleared via null-guards — see gotcha #45.)

### Security advisor gate *(run after ANY DB / table / policy / function change)*

Run the Supabase security advisor via MCP — `get_advisors { project_id: "ejpsprsmhpufwogbmxjv", type: "security" }` — and confirm it's **GREEN**. Acceptable remaining lints: the intentional `rls_enabled_no_policy` INFO on the deny-all tables + the `extension_in_public` (`pg_net`) WARN. **Any new `rls_disabled_in_public` / `sensitive_columns_exposed` (ERROR) or `rls_policy_always_true` (WARN) means a table regressed to anon-reachable → STOP and fix before committing.** See SECURITY INVARIANTS (top of this doc) + gotchas #141/#143.


```powershell
$logins = (Select-String -Path supabase/functions/vfo-admin-api/index.ts -Pattern '^\s*if \(action === ' -AllMatches).Matches.Count
$dispatch = (Select-String -Path supabase/functions/vfo-admin-api/router/dispatch.ts -Pattern '^\s*"([a-zA-Z0-9_]+)":\s*\(c\)' -AllMatches).Matches.Count
"action count: $($logins + $dispatch) (must match LIVE STATE table above)"
```


### Pipeline smoke gate *(run after any vfo-admin-api deploy, or after editing shared routing/dispatch/webhook/auth code)*

`vfo-edge-functions/scripts/smoke-pipelines.ps1` — authenticates as an admin, then fires one **read-only** loader per pipeline (`automation_load_pipelines` / `automation_load_tax_plans` / `automation_load_advisor_pipelines` / `automation_load_accountant_pipelines` / `automation_load_pip_pipelines`) and asserts each returns HTTP 200 + no `error`. Wiring check only — proves auth + dispatch + handler wiring across MAP 1 / Tax / Advisor / Accountant / PIP; does NOT exercise payment chains or per-automation logic. Read-only (no emails/charges/writes); exit 0 = all PASS, 1 = a pipeline broke. **⚠️ As of v472, all 5 of these loaders are in `SUPERADMIN_ONLY_ACTIONS` (the Automation-tab Jake-only lock) — run the smoke gate with a SUPERADMIN (Jake) token/creds, or every pipeline 403s and the gate reads as a false failure.**

```powershell
$env:VFO_SMOKE_TOKEN = "<SUPERADMIN/Jake session token>"   # or $env:VFO_SMOKE_EMAIL + $env:VFO_SMOKE_PASSCODE (must be Jake's — loaders are superadmin-only)
& C:\vfo-edge-functions\scripts\smoke-pipelines.ps1   # defaults to the live function
```

No secrets are hardcoded — creds/token come from env vars at run time. Skip for frontend-only, doc-only, or isolated single-handler changes.

---

## SMOKE TEST PATTERN *(PowerShell)*


```powershell
function Test-Action {
  param($name, $payload)
  $payload | Out-File -FilePath tmp.json -Encoding ascii -NoNewline
  $r = curl.exe -s -w '|HTTP %{http_code}' -X POST 'http://127.0.0.1:54321/functions/v1/vfo-admin-api' -H 'Content-Type: application/json' --data-binary "@tmp.json"
  Remove-Item tmp.json
  "$name -> $r"
}
'{"action":"admin_login","email":"dev@local.test","passcode":"dev2026"}' | Out-File tmp.json -Encoding ascii -NoNewline
$tok = (curl.exe -s -X POST 'http://127.0.0.1:54321/functions/v1/vfo-admin-api' -H 'Content-Type: application/json' --data-binary "@tmp.json" | ConvertFrom-Json).token
Remove-Item tmp.json
Test-Action "load_data regression" ('{"action":"load_data","token":"' + $tok + '"}')
```


---

## WHAT'S NOT IN THE SYSTEM

- No automated tests; no test runner; no `__tests__/`
- No GitHub Actions / CI/CD on either repo
- No frontend TypeScript (only the backend Deno code is `.ts`)
- No Supabase Auth — custom session-token scheme over `admin_sessions` (shared by **all three** login types). Passcodes are **salted PBKDF2-HMAC-SHA256** (210k iterations) stored in `allowed_admins.passcode_hash` / `member_logins.passcode_hash` / **`client_logins.passcode_hash`** (new this session — the third login type; see gotcha #85). The legacy unsalted-SHA-256 `passcode` column was dropped 2026-05-29 (gotcha #47).
- No React Context — but `src/lib/api.js` DOES hold a small module-level promise cache for read-only actions (caches `load_data` + `msm_load_programs` + `msm_load_*_track` etc. across re-mounts within a session; cache invalidates on `clearSession`). This is different from a state library — it's an in-flight-promise dedupe layer, not a state store.
- DB migrations directory exists at `vfo-edge-functions/supabase/migrations/` (mostly applied via Supabase MCP `apply_migration`, not via CLI auto-management). Five manual-apply SQL files live at `vfo-edge-functions/supabase/cron/{revshare,tax-revshare,chargescheduled,check-reminder,advisor}-sweep.sql` — applied once each to install the daily crons; not auto-managed. PIP Meetings has no cron.
- **Five background jobs** via `pg_cron` (02:00 / 02:30 / 03:00 / 04:00 / 05:00 UTC) — see CURRENT STATE. MAP 1 reminder timers live on `pipeline_map1`: `c14_email_sent_at` (PCADMIN Undecided email — only written on Undecided branch by `automation_PIPFU_decision`), `c17_followup_sent_date` (agreement signing — written by `automation_CONTRACT_sendagreement`), `pay1_email_sent_at` (payment email — written by `automation_CONTRACT_paymentemail`). Each stall has `*_reminder_sent_at` (48h idempotency guard) + `*_pf_notified_at` (96h idempotency guard) — six timestamptz columns total per row. The legacy unimplemented `c14_followup1_sent` / `c17_followup1_sent` / `pay1_followup1_sent` / etc. columns were DROPPED in migration `map1_reminder_ladder_columns` (2026-05-21). The newer `pay{2,3,4}_reminder_sent` columns are unrelated to the reminder ladder — they're 7-day pre-due-date nudges for check clients, written by `_checkreminder_sweep`. Tax reminder timers live on `client_tax_plans`: Tax 3 uses `tax_decision_reminder_sent_at` / `tax_decision_pf_notified_at` (Undecided email stall), `signed_reminder_sent_at` / `signed_pf_notified_at` (agreement signing stall), `payment_reminder_sent_at` / `payment_pf_notified_at` (payment link stall) — all 48h/96h tier; Tax 4 uses `post_review_reminder_sent_at` / `post_review_pf_notified_at` (Undecided 48h/96h; the Continue branch uses 24h auto-lock without a reminder column); Tax 5 uses `implementation_reminder_sent_at` / `implementation_pf_notified_at` (Undecided 48h/96h; the Proceed branch uses 24h auto-charge without a reminder column). Tax 4 meeting-date nudge to Tim uses `tax4_meeting_date` + `tax4_meeting_reminder_last_sent_at` (one nudge per UTC day until a Tax 4 decision is recorded). Advisor reminder timers live on `advisor_onboarding`: `decision_email_sent_at` (Undecided email stall — also the trigger for the 14-day implicit-No), `agreement_sent_at` (signing stall), `payment_link_sent_at` (payment stall). Each stall has `*_reminder_sent_at` / `*_pf_notified_at` 48h/96h guards — six timestamptz columns. The 14-day implicit-No rule shares the `decision_email_sent_at` timer with the Undecided reminder; it writes `final_decision='Auto-Declined'` + `final_decision_at` and chains `_declineemail`. PIP Meetings has no equivalent timers — there are no client-side reminder paths.
- No structured logging (just `console.log`/`console.error`) — **but frontend errors are now captured by Sentry** (H3, error-monitoring only; `@sentry/react` init in `main.jsx`, reported from `ErrorBoundary`; added 2026-06-18)
- No environment-specific configs for the frontend besides `VITE_API_URL` (Supabase URL + ANON_KEY are hardcoded)
---

## SESSION-LEARNED GOTCHAS

The **full numbered registry (#1–#248)** lives in **[GOTCHAS.md](GOTCHAS.md)** — read on demand. Numbers are permanent and referenced as `#N` across the docs; never renumber, and add new gotchas there (incrementing from #241). Below is only the curated **ALWAYS-APPLIES** subset — the invariants that bear on most work. A `see #N` reference resolves to the full entry in GOTCHAS.md.

**Deploy / build / ops**
- **#10** — `boldsign-webhook` deploy needs `--no-verify-jwt` every time (else BoldSign gets 401).
- **#74 / #112** — local `deno check` writes a v5 `deno.lock` the Supabase CLI bundler rejects; use `--no-lock` + `rm -f deno.lock` before `supabase functions deploy`.
- **#134** — JSX must live in a `.jsx` file; the `vite build` / `npm run deploy` rollup build rejects JSX in a `.js` file (the dev server tolerates it).
- **#193** — a prop/var used only inside a NESTED component that doesn't receive it throws a runtime `ReferenceError` that `npm run build` does NOT catch; when threading a new prop, pass it into every nested sub-component that uses it and click-through — a green build isn't proof.
- **#145** — production deploys (`supabase functions deploy` / `npm run deploy`) need an EXPLICIT "deploy"; ambiguous approvals ("continue"/"yes") are blocked.
- **#146** — the Bash tool's cwd PERSISTS across calls — `cd` to the correct repo/worktree (or use absolute paths) immediately before a deploy.
- **#196** — every migration applied live via the Supabase MCP (`apply_migration`) MUST also be committed as a file in `supabase/migrations/` in the same session — an MCP-applied migration exists only in the DB, and a rebuilt/branched environment silently lacks it.
- **#223** — MCP `execute_sql` with multiple `;`-separated statements returns ONLY the LAST statement's result (earlier ones execute silently); run verification SELECTs separately or fold checks into one statement.

**Auth / security invariants** *(see also the SECURITY INVARIANTS box above)*
- **#141** — every new public table = RLS enabled + a `"Deny all access" USING(false)` policy in the SAME migration, then verify with an anon-key REST probe.
- **#142** — service-role bypasses RLS → authorization is 100% app-layer; member-facing AUTH handlers MUST re-check ownership against `auth.callerMemberNumber`, never a body id.
- **#143** — new SECURITY DEFINER function = pin `search_path` + `REVOKE EXECUTE … FROM public` (revoking from anon/authenticated alone is not enough).
- **#11** — chain auth: a handler chaining to another AUTH handler must forward `body.token`, not just the `Authorization` header.
- **#144** — frontend `callApi` hard-redirects on ANY 401 except `LOGIN_ACTIONS`; add new login-type actions to that allowlist.
- **#171** — Lost/Removed members are BLOCKED at `member_login` (403 after passcode verify); never delete `member_logins` rows to revoke access — flip `elite_status` instead, and remember the shared Test Member must stay Active for the test login to work.
- **#167** — the Accounting / Automation / Member Overview tabs are per-admin tab-gated via `allowed_admins.allowed_tabs` (NOT superadmin-only anymore); a NEW action behind one of them MUST be added to `TAB_ACTIONS[...]` in `role-gates.ts` or a granted non-superadmin admin gets 403. Grants apply on the admin's next login (session-stamped).

**Backend data patterns**
- **#28** — PostgREST `.neq('col', v)` EXCLUDES NULL rows; use `.or('col.is.null,col.neq.v')`.
- **#84** — Storage writes from the edge fn MUST use the supabase-js storage client, not a hand-rolled `Authorization: Bearer <SERVICE_ROLE_KEY>`.
- **#204** — the ERT/VFOS vault is admin-write / owner-read: it lives in its own `ERT_BUCKETS` map + admin-only `admin_ert_*` handlers; owner READS ride `*_vault_list`(+ert)/download(`section='ert'`) but owner WRITE handlers MUST reject `section='ert'` (the `*BucketFor` helpers default unknown sections to general).
- **#205** — the BoldSign doc-id column name differs per pipeline (`boldsign_doc_id` MAP1/Tax vs `boldsign_document_id`/`lic_boldsign_document_id` onboarding); the sign+pay→ERT copy (`utils/ert-agreement-copy.ts`) fires at entity-create/first-payment, guarded by `ert_agreement_copied_at`, destination = the payer's vault.
- **#206** — Supabase blocks direct SQL `DELETE FROM storage.objects` (`storage.protect_delete()`); delete vault files via the Storage API (`admin_ert_delete` / dashboard), not MCP SQL.
- **#221** — sign-and-file into the ERT vault via `copyAgreementToErtOnce()` must pass `name` so the file reads `…-Agreement-First-Last.pdf`; all 5 callers do — a new caller that omits it falls back to the bare label. Only affects agreements filed after deploy (the `ert_agreement_copied_at` guard).
- **#92** — allocate INV/REC numbers via collision-safe `utils/doc-numbers.ts allocateDocNumber()`; never revert to insert-once.
- **#45** — `deno check` baseline = 0; any error = fail (the VERIFICATION GATE depends on this).
- **#155** — `members.member_category` + `member_number_baselines` both carry CHECK constraints (advisor/accountant only); a new category needs the `members` CHECK widened and its numbering baseline kept as a code constant, not a baselines row.
- **#187** — ALWAYS destructure `{ data, error }` (never `{ data }` alone) from a `supabase.from(...)` call in webhook/automation code and check `error` — otherwise a real DB failure is indistinguishable from "no matching row" and fails 100% silently with no log trace.
- **#215** — PostgREST MULTI-ROW inserts unify keys across the array and fill missing keys with NULL (NOT the column default) — every row in a bulk `.insert([...])` must carry the identical key set or a NOT NULL default column blows up.
- **#244** — `extra_cc` stores comma-separated TEXT but the decision forms submit ARRAYS: every writer must normalize (join) and every reader must parse via `utils/extra-cc.ts extraCcList()` (never a bare `.split(",")`) — a raw array write serializes as a JSON string and Gmail 400s the whole email ("Invalid Cc header").
- **#217** — edge-function `console.log/error` output is NOT visible through the MCP `get_logs` (request lines only); debug a live webhook path with a temporary deny-all trace table, then drop it.
- **#188** — if a write to an established column suddenly fails with "column does not exist" (42703) while reads/other-writes on the same table work fine, suspect a PostgREST schema-cache split before a code regression; `NOTIFY pgrst, 'reload schema'` may not fix it — route that write through a `SECURITY DEFINER` RPC instead of a plain REST `.update()` (RPC only needs the function name resolved, then runs as native SQL with full column visibility).
- **#230** — the Payment Continuation backfills hard-guard collisions (409 + `force`, trusting `legacy_source='old-system'`), but RAW SQL bypasses everything — hand/visual migration must follow `C:\Users\jakel_fjetgbx\visual-migration-rules.md`: payment columns belong to the tool; visual writes touch progress tables only; decision columns are inert WITHOUT their `*_email_sent_at` twin; `presentation_send_date` + bare `tax4_meeting_date` are email booby-traps. `migration_setup_tokens` are single-use (`used_at` consumed by the `card_update` webhook; connect-checkout 410s used tokens).

**Stripe / payments**
- **#13** — the Stripe webhook routes by `metadata.pipeline` + `metadata.payment_kind`; set both on every Checkout / PaymentIntent you create.
- **#15** — Stripe idempotency keys must auto-rotate per retry attempt, not be reused.
- **#159** — before a Stripe Connect transfer, verify the destination's `capabilities.transfers === 'active'`; a created-but-unonboarded account rejects transfers — treat it as "awaiting setup" (resend link), not a failure. A feature's Connect accounts + transfers must run in the SAME sandbox/live mode.
- **#198** — every Stripe subscription creation emits a $0 `billing_reason=subscription_create` invoice that finalizes AND auto-"pays"; any invoice-event consumer routed by customer id must skip `amount_due <= 0` or setup day fires the paid chain on a bogus $0 row.
- **#211** — Growth Credits fulfillment: `checkout.session.completed` fulfills ONLY when `payment_status='paid'`; ACH lands via `checkout.session.async_payment_succeeded`, which MUST be subscribed on the Stripe endpoint (both modes); idempotent via `gc_transactions.stripe_session_id`; prices come only from the server-side `GC_PACKAGES` map.
- **#228** — off-session recurring-charge idempotency keys must be LOGICAL/date-less (`chargescheduled-<client_id>-P<n>`); a date suffix let a lost post-charge status write re-charge the same installment the next night (ACH especially). Always error-check the post-charge status write. Refines #15 (rotate on RETRY, never on a charge's logical identity).
- **#229** — `payment_intent.payment_failed` for OFF-SESSION charges is a LATE-ACH signal, not a duplicate: an off-session ACH returns 'processing' and can bounce days later. v612 adds MAP1-installment + TAX-implementation branches keyed on the 'processing' status guard; any new off-session ACH charge path needs its own late-failure consumer.
- **#235** — membership renewal invariant: every year-1 schedule row must sort strictly BEFORE `renewal_date` (the sweep's renewal guard treats any row on/after it as an already-generated year 2) — day-15 payers renew at pay+12mo, not the "last 15th strictly before". `see #235`.
- **#236** — membership pull-failure side effects (suspend + bell + email) must fire on BOTH the sweep's sync card path AND the webhook's late-ACH path, via shared `utils/membership-payment-failure.ts`; in the `MEMBERSHIP_payment_failed` template `[Failed Amount]` (failed charge total) and `[Amount]` (per-pull) are DIFFERENT tokens. `see #236`.

**Notifications**
- **#17** — notification rows use column `message`, NOT `body` (writing `body` inserts NULL → blank bell rows).
- **#18** — notification `link` must be a real frontend route (e.g. `/admin/client/<id>?tab=tax`), or the bell click bounces to login.
- **#176** — EVERY bell insert goes through `utils/notify.ts notifyByRule(sb, key, {...})` + a `notification_rules` seed row; raw `.from("notifications").insert()` bypasses the Notification Editor and is a regression. Sweeps skip a disabled tier BEFORE stamping its idempotency column.
- **#178** — deleting a `notification_rules` row does NOT silence the notification (missing rule = code default fires); disable = `enabled=false`; permanent removal = delete row + revert the code call.
- **#179** — `mark_notification_read` refuses `dismissible=false` rows; action-required bells clear ONLY via their completing handler's direct DB update.
- **#181** — every NEW templated email = an `email_templates` row + a `TEMPLATE_META` entry (plain-English label / recipients / process position) in `components/shared/templateMeta.js` (moved out of `EmailTemplatesPanel.jsx` 2026-07-20, which now imports it) + delivery via `gmailDraftFetch(...)` or `draftGmail({..., sendMode})` so the per-template Draft/Send toggle (`email_templates.send_mode`) applies; a raw Gmail drafts.create fetch bypasses the toggle and is a regression. (Email twin of #176.)
- **#248** — every NEW email-sending step gets a `StepEmailsChip` (the inline "Emails (N)" pill + preview modal): pass the EXACT `email_templates.template_name`(s) the step can send + a `context` of known token values, and `!readOnly`-guard it on member-visible tracks. `mv`/`TEMPLATE_META`/`ROLE_LABELS` live in `components/shared/templateMeta.js` (shared with `EmailTemplatesPanel`); the chip loads previews via the plain-admin `load_email_previews` action (silent fallback `automation_load_email_templates`). `see #248`.
- **#213** — "clearing" a notification only flips `read=true`; read rows live in the Notifications page Archive and are hard-deleted after 90 days by the daily purge cron; personal reminders insert `notifications` rows DIRECTLY (the one documented exception to #176 — user-authored, pipeline `REMINDER`).
- **#224** — advisor/accountant onboarding notifications carry NO `client_id`; an action-required onboarding bell MUST embed `onboarding=<id>` in its `link` and be cleared by its completing handler via pipeline + `dismissible=false` + `link LIKE '%onboarding=<id>%'` — a bell whose link lacks the id is permanently unclearable.

**Frontend / repo hygiene**
- **#30** — `callApi` retry rule: reads (`^load_|_load(_|$)` + `vault_list`) retry once on timeout; writes NEVER retry (prevents double-writes).
- **#225** — the track tabs' `formatDate` splits a `YYYY-MM-DD` string (→ `MM/DD`); a `timestamptz` fed to it renders mojibake (`07/15T00:54…`) and can be a UTC-day off. Display timestamptz columns with a local formatter (`formatStamp`), not `formatDate`.
- **#182** — `location.state` dies on browser reload: every view (portal tab / program / sub-tab) must be recoverable from the URL (`ClientDetail` self-heals `?program=&tab=` via `window.history.replaceState` — never `navigate`/`setSearchParams` for self-heals) or sessionStorage (`adminActiveTab`/`memberActiveTab`/`specialistActiveTab`/`clientActiveTab`); a NEW portal tab/view must follow one of the two patterns, and logo-home handlers must clear/reset the restore keys.
- **#212** — AdminPortal `?tab=&section=` deep links are consumed then STRIPPED via `history.replaceState('/admin')`, and the effect reads `window.location.search` (router state goes stale after replaceState); a new deep-linkable tab must follow or reload re-hijacks the user.
- **#214** — note `visibility` ('internal' | 'shared') is enforced in the LOADERS by caller role (member reads filtered/redacted; member writes forced 'shared'); the amber (Internal) / teal (Shared with member) tag colors are RESERVED for visibility across all note UIs.
- **#36** — `docs/` exist in BOTH the main checkout AND every worktree; only ever edit the worktree copy.
- **#169** — the frontend is themed: new code colors via `var(--vfo-*)` only (navy TEXT = `var(--vfo-heading)`; navy in gradients stays literal; brand blues + status colors fixed); new portal-level pages call `usePortalTheme()`, public/login pages stay light.
- **#151** — in a Vite `index.html`, reference `public/` assets as a plain `/file.svg` (Vite prepends the base once); `%BASE_URL%file` double-bases → `/vfo-portal/vfo-portal/…` 404 → stale favicon.

**Specialists / showroom visibility** *(Feature C)*
- **#147** — hide rows from a SHARED data source in the rendering COMPONENT (`MemberShowroom` filters to Active), not only the loader — admins get the full set by design and the admin preview reuses the same component.
- **#148** — the public website widget reads `experts` directly via the anon key + the `"Public read"` RLS policy; hide rows from it by tightening that policy (`USING(status='Active')`), not a handler.
- **#149** — convert empty date-input strings to `null` before any `update({...form})` passthrough save (Postgres rejects `""` for a date column).
- **#162** — an ecosystem name lives in 5 places (the `vfo_ecosystem_assignments` rows + 3 hardcoded frontend arrays + the `vfo-widget.js` bucket file, source now tracked at `widget/vfo-widget.js` in the edge repo); rename/add = touch all, and the widget's hardcoded filter buttons silently break (0 results) if its copy isn't re-uploaded. The in-portal showroom is one shared `MemberShowroom` gated by a `showMemberServices` prop.
- **#201** — the public showroom widget reads `experts` via the PUBLIC anon key, column-restricted to safe columns (`id,name,short_bio,long_bio,headshot_image,status,sort_order` + `vfo_accredited` as of 2026-07-16 → 8 columns); a new widget column needs a matching `GRANT` (migrations `20260709160000_experts_anon_column_grants.sql`, `20260716000000_experts_vfo_accredited.sql`) or the fetch 401s. `email`/`stripe_account_id`/`revenue_decision`/`D&B_*`/`ecosystem_content` are NOT anon-readable; the portal is unaffected (reads via the edge fn on `service_role`).
- **#231** — `experts.vfo_accredited` (VFO Accredited Professional Specialist) is hidden from EVERY showroom + the widget but shown in Specialist Search + the Holistic/Tax pickers; the hide is a `MemberShowroom.jsx` filter (all 5 portal surfaces) + a mirrored `vfo-widget.js` filter, NEVER a `load_data` strip (which would break the pickers). A new showroom surface routes through `MemberShowroom`; a new anon specialist reader replicates BOTH the `Member Services` and `vfo_accredited` exclusions. `see #231`.
- **#232** — `experts.ecosystem_content` is an ordered ARRAY (multiple entries per ecosystem allowed), read via `entriesFor()` which also handles the legacy object/null shapes; entry 0 is primary (its `D&B_*` mirror the flat columns). The flat `short_bio` = the DISTINCT entry short bios joined `\| ` (showroom card/modal + widget + admin list read it, so multi-ecosystem specialists show every specialty). Any new `ecosystem_content` consumer must handle the array, not name-keyed lookup. `see #232`.
- **#220** — one specialist can serve multiple ecosystems with a DIFFERENT `short_bio` + non-tax `D&B_*` each, stored in `experts.ecosystem_content` jsonb; the flat top-level columns MIRROR the primary (first) ecosystem (widget/showroom/agreements read the flat columns, so keep them in sync on save). Tax fields are SHARED (flat columns only, shown under the `Tax Planning` ecosystem only). Dedupe every ecosystem list from `ecoMap` with `[...new Set(...)]` — `vfo_ecosystem_assignments` has no unique constraint. `save_specialist`/`load_data` pass the whole `expert` through, so a new `experts` column needs no handler change.

**MSM / training track (90 Day Plan)**
- **#233** — training-track (`program_training_tasks`) completion is POSITIVE-ONLY and lives in ONE shared module `components/shared/trainingStatus.js` (admin `MSMTracking.jsx` + member `MemberMSMTracking.jsx` both import it — edit once, never re-implement). A task counts toward progress/greens a phase only on a positive status (denylist `NOT_DONE_STATUSES = {Outstanding, Will Watch, Pending, In Progress, Stopped}`; everything else is done). `N/A` drops a task from numerator AND denominator; any `Stopped` step makes the "90 Day Plan:" header read Stopped (live). A new not-done option must be added to `NOT_DONE_STATUSES` or it silently counts as done. The client track (`program_client_tasks`) is a separate vocabulary and does NOT use these helpers. `see #233`.
